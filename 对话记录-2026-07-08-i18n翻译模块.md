# 对话记录 2026-07-08 —— i18n 翻译模块：去 git 指纹 / U-1·U-2·U-3 落地 / 简化 / 手把手测试

> 权威决策见 `CLAUDE.md` 决策日志（2026-07-07/08）与 `docs/blueprint/DECISIONS.md` #7/#8/#9。
> 本记录补充**会话叙事 + 操作知识 + 踩坑**，不重复正式决策条文。

## 本次提交（按序）
| commit | 内容 |
|---|---|
| `124608f` | refactor：`touch` 变更检测**去 git** → MD 自存指纹 `i18n_fp`（决策㉗落地） |
| `e0df59f` | feat 阶段A：术语/锁词表 `src/i18n/terms.zh-CN.en.yaml` + emit 注入 + lint |
| `93ea99a` | feat 阶段B：可翻译名册校验器 `i18n:validate`（挂进 check） |
| `58bfaa4` | feat 阶段C：发布门禁 `isPublishable` + 镜像结构同步 `--sync-structure` |
| `e487884` | docs：DECISIONS #7/#8/#9 OPEN→RESOLVED + CLAUDE.md 决策日志 |
| `2558da5` | refactor：**删 lint 层**，terms 表只当翻译提示词 |
| （本条） | docs：对话记录 + terms 头注释/gitignore 收尾 |

## 关键决策与理由（速览）
1. **去 git 指纹**：`touch` 原用 `git show HEAD` 当基准比字段变化，违背"git 只存代码、内容真相在 AWS"。改为 frontmatter `i18n_fp:` 存"上次抬戳时各字段值的 sha1 前 12 位"。不变式由 `stampOnSave`（编辑器路）与 `touch`（兜底）共同维护，混用不会重复抬戳。至此 i18n"找改动"全链零 git（残留 `gitActor()` 只取操作者名，属归因非检测）。
2. **U-1 不翻译**分两层：①整字段（图/URL）= 推导集合自动排除 image/link，`i18n:validate` 校验手埋名册 ≡ 推导（先校验后推导）；②行内保护词（型号/品牌）= terms 表 `lock`。
3. **U-2 术语表**与 U-1 行内词合一（terms 表 lock+map）。
4. **U-3 部分翻译态 = 上次英文 + 发布门禁**：过期字段照渲上次英文（渲染层零改动）；有"从未翻译"字段的目标页被 `build.mjs::isPublishable` 扣下（不进 dist/sitemap/hreflang）；源新增段用 `--sync-structure` 灌源占位。**否决蓝图原"藏掉"倾向**。
5. **删 lint（07-08 简化）**：用户指出对"能读指令的 AI 翻译节点"属过度设计——AI 看提示就遵守、lint 每次都过，且逐字校验误报率高（初版对合法变体误报 81 次）。删掉整个 lint 层，terms 表回归**纯提示词**。日后若换 Google 翻译这类笨 MT 再加回。

## 翻译节点：现状 vs 系统版（本次讨论重点）
- **现在翻译 = 人工 AI（对话里手翻）**，没接任何翻译 API、没有事件自动触发。`--emit`→翻→`--from` 三步是**本地把内部接缝手动裸露**的样子。
- **系统版（M3）会塌成一步、全自动**：编辑器保存 → 保存事件触发翻译 Lambda（内部就是 emit 逻辑）→ 自动调翻译服务出译文 → 自动写回 S3 + 抬戳（from 逻辑）→ 触发重渲发布。**运营只做：改中文、保存；英文自动跟上**（或进审核队列）。emit/JSON/from 永不露给用户。
- 迁移成本 = 换 `loadTerms`/内容读取的 reader（本地文件→S3 GetObject）+ 外面包事件触发，**内核纯函数一行不改**。

## 手把手测试演练（操作知识 + 踩坑）
完整回路的手动跑法：`i18n:status`（看待翻）→ `--emit`（拿坐标+源文本+术语提示）→ 人工翻译成 `{"字段":"英文"}` JSON → `--from <文件>`（代码按坐标写回+抬戳）→ `i18n:status` 转绿。
- **踩坑1「改了没显示 stale」**：改**英文页**不加源戳（设计如此，目标页=人工润色）；只有改**中文源的追踪字段**才触发。判据看跑 `edit` 那个终端有没有 `⟳ i18n_rev.xxx=N`。
- **踩坑2「编辑器直接改英文不转绿」**：编辑器改 en 只改内容、不抬 `translated_rev`；要正式记为"翻译完成"必须走 `--from`（只有它抬戳）。
- **踩坑3「垃圾进垃圾出」**：源里有测试废话"你好呀呀"，翻译这一步不会替你清理——要么带进英文、要么人工丢掉；**正确修法是删中文源，不是指望翻译判断**。演示时用户选 B（原样翻），英文里就真出现了 `Hello there~`。
- **中转文件不入库**：`--from` 用的译文 JSON 是**一次性交接单**（用完即弃），译文最终归宿是 `src/content/en/*.md`；该 JSON 放 `/tmp` 或 gitignore，不进仓库（MD 是资产、它是耗材）。

## 现状 / 遗留
- **翻译机制层面：完整、验证过、零 git，收工**。别再往里加（lint 就是过度设计的例子）。
- 要**英文站真上线**还差两件（非翻译模块的锅）：①**语言切换器是死的**（WordPress 残留，指向错误旧页，访客点不过去）——需由 `i18nGroups` 驱动；②只有 5-ton 一页有 en 镜像，**批量铺开**未做（新建整页镜像目前手动，值得加 scaffold）。
- U-4 两道校验（hreflang 对称 + 镜像结构一致进 check）登记未做，但当前单语言对下价值低（hreflang 由 seoHead 对称生成、结构上保证）。
- 站点层策略常量（`SEO_ALLOWLIST`/`STRUCTURAL_EXCLUDE`）终态迁 `site.config`；terms 表成熟系统大概率随内容搬 S3（换 `loadTerms` 一个 reader）。

## 下一步候选（用户定向）
1. 语言切换器接活（让 chrome 语言链接指对 en 页）。
2. 多语言批量铺开（更多页做 en 镜像）。
3. 自动化翻译（接 API 当中间节点）——非必要，页面少时手翻质量更高。
4. 转 EXECUTION phase 1 其他缺口（丢 Python / single-girder 收敛超集 / 404·favicon）。
