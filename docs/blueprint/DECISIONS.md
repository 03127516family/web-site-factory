# 蓝图决策日志（DECISIONS）

> 蓝图侧新拍定的约定记在这里，带日期追加、只增不删。**CLAUDE.md 由用户本人维护，AI 不改动**（2026-07-03 用户明确）——此前 AI 误往 CLAUDE.md 决策日志追加过一条，已恢复原样，内容移入本文件。

---

- **2026-07-03 ① 全流程蓝图 v2 落成**（`docs/blueprint/` 01–09 篇，内核部分），取代 2026-06-19 spec（两份旧 spec 已加追记封存，仅作历史）。权威顺序：**代码实际行为 > CLAUDE.md（用户维护）> 本蓝图 > 旧 specs/对话记录**。随蓝图拍定三处内核流程修订：
  1. **生产路收敛为一条**：B 路（AI 直写 dist）由"逃生舱"降为**封存**——multi-point 已实证 B 路产物自行收敛回 render 路，且 B 路破坏"编辑写回 MD 后出货页自动重渲"的闭环。特殊版式一律优先"每页一份骨架模版"（posts A 路）承接；启用 B 路须用户特批。
  2. **注册表搬家触发已到**（原约定时机="做第二个页族时"，posts 即第二页族）：`REPEAT_FM_ARRAY`/`NO_STRUCT_EDIT` 搬进模版 `data-array`/`data-no-add`、退役 legacy 双模版，列 P2、排在内容搬迁之后。
  3. **停止准则**（01 章）：完成线未过不开新组件；"做内容永远合法，做工具必须挂在里程碑清单里"。
  另：巡检发现**仓库无任何 git remote**，全部资产唯一副本在本机——P0 风险，建私有远端先于一切功能工作。

- **2026-07-03 ② 产品化定位拍定（方向修正，当天第二条）**：用户澄清——现仓库只是**内核的参考实现 + 首站（dgcrane）资产**，给 AI 看是为了理解"我们在做的东西"；目标是做一个**脱离本机的产品**：运营在浏览器里"贴内容 → AI 云端生成 → 可视化审核点改 → 一键发布静态站"，全程不碰命令行、不依赖任何特定电脑。**Why**：蓝图 v2 初稿把"现状"当成了"终点"，所有流程停在本机 `npm run`，没有回答"往后做成什么"。**How to apply**：
  1. 蓝图新增产品化两篇：[10-产品化形态与路线](10-产品化形态与路线.md)（产品定义、角色、内核动作↔产品形态映射、里程碑 M0–M3）、[11-产品化架构](11-产品化架构.md)（托管架构、生成 worker、状态机、安全、演化路径）。01–09 篇定位为**内核篇**，规范与原理继续有效。
  2. 01 章触发表两项转为主线：托管编辑器 → **M1**；建页薄壳 → **M2**（且不再是薄壳，是产品本体）。其余触发项仍由信号驱动。
  3. 05 章 SOP 的 npm 命令 = 内核动作的**开发态**形态，保留为 M1 前的操作手册与永久调试态/逃生舱；产品态逐条映射见 10 章 §4。
  4. CLAUDE.md 恢复原样；蓝图侧决策此后记本文件。

- **2026-07-03 ③ 内容生命周期补章（12 章）**：回应用户问询"后续维护、编辑修改、修改后如何同步相关语言"。盘点结论：编辑/修改已覆盖（01 三级模型 + 05 SOP + 10 映射）、维护偏薄（仅 01 §7 节拍）、**多语言同步只有 09 §2 草案**。新增 [12-修改传播与多语言同步](12-修改传播与多语言同步.md)，拍定：
  1. **改动传播矩阵**为运营期因果地图——系统只有八种改动，每种的下游反应可枚举；出现第九种先补表再动手；**slug 变更列为专门流程**（唯一打断外链/索引的改动）。
  2. 多语言正式机制：**per-page `i18n.source_commit`** 过期检测（取代 09 §2 全局 tag 草案）+ 解析后结构层字段级 diff（内核命令 `i18n:diff`，零 AI）+ 翻译任务**只重写源变更字段**。
  3. **人工润色保护**：源未变的字段机翻永不触碰（机制保证）；源变且目标被人工改过 → **冲突待人工**，不自动覆盖。
  4. 版式/模版全语言共享 → 换版式零翻译成本；每语言独立审核发布；hreflang/sitemap 由 pages[]×locale 同源生成。
  5. 现在（M0–M2）只需守四条预留：`/zh/` 前缀、marker 无语言、fragments 可拆语言目录、`i18n.*` 字段名——守住即随时可启用、零返工。

- **2026-07-03 ④ 完整性审计（回应"是否已是全部流程 / 能否替换 WordPress"）**：拿"完整网站生命周期 + WordPress 为本站提供的每项能力"逐项对照蓝图。结论：**生命周期框架闭环**（进料→生成→编辑→验证→发布→回滚→多语言→维护，每环有章可查），但补了 4 个真实小缺口：①01 章 v1.0 清单新增第 7 条"站点杂项"（404 页、统计埋点、favicon、评论/RSS/定时发布的不做判定留档）；②导航菜单未数据化 → 04 章债务台账#7 + SOP-1 加"核对入口"步骤（短期手工核对，长期由 pages[] 生成菜单）；③托管编辑器补"媒体上传 = 图片闸门产品化"（11 章 §2.3）；④判定：评论/RSS/定时发布为 B2B 目录站不需要项，真需要时分别走第三方嵌入 / CI 计划任务，不进内核。**"能否替换 WordPress"的判定标准 = 01 章 v1.0 清单全绿，不是理论完备**——当前卡在待拍板 #1–#3 与搬迁执行，均为执行项、无未解设计问题。

- **2026-07-03 ⑤ 第二轮完整性审计 + AI 必须性拍定**：换法务合规/规模化/AI 组件化三个视角复查，补 6 处：① ICP 备案号页脚展示（若国内托管）+ 法务页（01 章清单第 7 条）；②素材**原件库**约定——仓库只放压缩出货图，原件存冷区按 slug 归档（08 章 §3）；③编辑器**粘贴清洗**——Word/微信粘贴一律 `htmlToMd` 归一化（11 章 §2.3）；④表单**反垃圾**必备 + 询盘数据归宿必须随方案拍定（07 章 §5.1）；⑤规模化触发——页面 >100 时增量构建/按需 geom（01 章触发表）；⑥**AI 质量管理四件套**：缺料不造料红线、生成回归集（golden set）、知识包版本化、人工修正率反馈环（11 章 §2.4）。
  **AI 必须性正式拍定**：**输入端必须 AI**（烧 MD、造模版、post 骨架版式、翻译、生成自检——本质是语义/设计判断，写不成规则，没有它们系统退化回"人肉排版"老路）；**运行端禁止 AI**（渲染、校验、发布、回滚、diff——必须确定性，用 AI 反而破坏可复现）；**中间可选**（SEO 描述初稿、alt 初稿、润色——都有确定性回退）。AI 形态必须是**带验证回路的 agent**（烧→validate→build→geom→修），不是裸 completion 调用。AI 同时是成本与风险项（LLM 通道、token、模型漂移），故有四件套管着。

- **2026-07-03 ⑥ SEO × 多语言交叉审计**：确认两者均已有设计（SEO=08 章、多语言=12 章、交叉点=12 章 §3.7），交叉处补 3 项：① hreflang 组加 **x-default** 指向默认语言页；② **语言切换器与 hreflang 同源**——切换链接指镜像页、未翻译时降级指对方语言首页，可见 UI 与 SEO 声明永远一致；③ `og:locale`/`og:locale:alternate` + JSON-LD `inLanguage`。另修正 08 章 §5 的过期指针（09 章 → 12 章 §3.7）。

- **2026-07-03 ⑦ 外贸站维度审计（回应"外贸网站包含哪些方面/动态数据/复用/站点管理/可见即可编辑"）**：补 6 处——
  ① **站点级内容 `site.md`**（04 章 §8，本轮最重要）：电话/邮箱/社媒/备案号等 chrome 高频值数据化，fragments 挂 `data-field="site.*"`——页脚电话从此运营点改，"可见即可编辑"覆盖到 chrome 值；12 章矩阵按自身纪律增补第九行。
  ② **动态模块通用协议**恢复为正式预留（07 章 §5.5）：`data-dynamic`+`data-slot`+加载/空/失败降级；铁律"API 挂掉不影响静态主体"。
  ③ **视频/附件形态注记**（04 章 §2）：视频嵌入=骨架（L3）+地址/封面值（L1）；PDF 下载=链接字段+附件规范（08 章 §3：`assets/files/`、≤10MB、语义命名）。
  ④ WhatsApp/微信浮窗 = 第三方挂件 + 站点级值（07 章 §5.3）。
  ⑤ 编辑器补**页面历史与一键回退**（保存=commit 的红利）与附件上传（11 章 §2.3）。
  ⑥ 多站点管理深化为**站点注册表**（11 章 §7）：每站独立内容仓库/发布目标/语言/基线，共享引擎/后台/账号；站点间物理零耦合。

- **2026-07-03 ⑧ 主动完备性总审计 + 审计协议固化**（起因：用户批评"提一个补一个"的被动模式成立）。两个动作：
  **(a) 结构性改变**——新增 [13-完备性审计协议](13-完备性审计协议.md)：六透镜（生命周期/角色/数据/失效/领域/对标）× 固定触发时机（每里程碑完成线前全套）× 强制记录格式（发现→处置三选一：立即修/挂账/显式不做）；并写进 01 章 v1.0 清单第 8 条。**完备性从"用户提问驱动"变为"协议驱动"**。
  **(b) 本轮六透镜扫描结果**（9 项，全部已处置）：①移动端双视口验收缺失→06 章视口策略（**此前 geom/预览只提桌面，响应式站的真实漏洞**）；②构建环境未钉死→06 章质量门第 5 条（Node pin/lockfile/容器镜像/双跑 diff，兑现确定性承诺）；③`/zh/` 前缀的资源路径策略与本地子路径预览→07 章 §2（防"本地全对上线全 404"）；④Cookie 合规判定（欧盟访客）→01 章清单第 7 条；⑤安全响应头+HTTPS 强跳→07 章发布检查单第 7 条；⑥可访问性基线→08 章（lang/alt/层级/对比度，静态站天然达标大半）；⑦域名续期提醒→12 章维护表；⑧token 成本月报并入质量反馈环→11 章 §2.4；⑨srcset 响应式图片显式判定为 P2 不预做→08 章 §3。
  **诚实边界**：协议保证系统性遗漏难以存活，不保证零遗漏；执行是最后的审计员，坑账/矩阵纪律/golden set 承接执行期新发现。

- **2026-07-03 ⑨ 门禁体系整合（回应"做到哪一步才能进下一步"）**：判据此前齐但**散**（01/05/06/07/09/10 各管一段，无总表）——判据散着=执行靠记忆串门禁，必漏。新增 [14-门禁与验收体系](14-门禁与验收体系.md)：**六级门禁梯子** G0 改动门（check 绿才许提交）→ G1 页面门（SOP+geom+入口核对+人审才进待发布）→ G2 发布门（检查单 7 条才上线）→ G3 批次门（上批全绿+本批前置才开下批，跨批不抢跑）→ G4 里程碑门（完成线+所需拍板才开下级，过线上级冻结；**里程碑是唯一必须用户亲自验收的门**）→ G5 扩建门（触发证据才开工）。附三原则：判据必须可验证、失败不许绕（`--no-verify` 须留痕补账）、**提前必须显式**（跨门提前项须 DECISIONS 标注，如 P0 远端属 M1① 提前）。随手收紧两处：09 章补批次门禁文字、10 章 §6 补显式提前条款。本章定位为地图，判据权威仍在原章。

---

- **2026-07-03 ⑩ 两项拍定 + 运行模式确立（用户问答结果）**：
  1. **托管 = 自有 AWS S3 + CloudFront**（07 章 §3 已更新）。剩余为事实项非决策项：账号（global / aws-cn）与区域、"可以建资源"的明确指令、DNS 归属与整域/子域接管方式、备案状态（仅国内加速）。红线（不动 AWS）维持到指令给出。
  2. **询盘表单 = 邮件路线，投递用 Postmark**（07 章 §5.1 已更新）：表单 POST → 轻量函数端点 → Postmark → 收件邮箱。待给：Postmark 账号/API key、发件域名 DNS 验证、收件邮箱。
  3. **发布授权档暂按最严**：AI 完成的一切工作，上线前用户过目点头（红线原样）；用户理解"分级授权"概念后可随时定档提速——已在 EXECUTION 置顶排队解释。
  4. **git 远端未拍**：用户疑问"git 存什么——网站肯定不在 git 里"。澄清已给：**产物（dist/网站）本就不进 git（.gitignore 实证），去 S3；git 存的是源**（内容 MD/模版/引擎/蓝图）——丢源等于丢一切。待用户理解后选托管商。
  5. **确立"自己跑"机制**：新增 [EXECUTION.md](EXECUTION.md) 执行看板——用户只看"等你的事"，AI 任何会话读它零解释续跑，每次会话结束前必须更新（新纪律）。

- **2026-07-03 ⑪ 用户指示：AWS 与 Postmark 实物暂缓，表单先 mock；GitHub 仓库启动**：
  1. AWS 授权包明确暂缓——deploy/rollback 只写骨架，不建任何资源，红线继续。
  2. 表单转 mock：前端实现完整提交体验（校验/加载/成功/失败四态 + honeypot），提交端点收敛为**单点常量 `INQUIRY_ENDPOINT`**，mock 阶段指向假地址；真 Postmark 函数就绪后改这一个常量即切真——**mock 与真端点走同一请求协议**（字段/编码/响应约定现在就按 Postmark 函数的目标形态定），保证切换零改造。footer 订阅表单（780）同治。
  3. GitHub 私有仓库由用户创建，仓库名推荐 **`dgcrane-site-factory`**（备选 `dgcrane-web`、`mdforge`），介绍文案见本条下方；建空仓（不初始化 README/.gitignore），地址给到后 AI 负责加 remote、推送、配 CI。
  > 仓库介绍（可直接粘贴）：`内容驱动的静态站生产系统：AI 把资料烧成 Markdown 真相源，确定性引擎渲染静态站，可见即可编辑，S3/CloudFront 发布。dgcrane 中文站 + 建站引擎 + 全流程蓝图。`

- **2026-07-03 ⑫ 产品定位确认：dgcrane 仅是首个试点，多站点 + 多语言是主线**（用户明示："dgcrane 只是第一个例子，以后其他站也要这个方式，且不止中文"）。影响：
  1. **待拍板 #4 半决**：「自家多站的内部工具」已确认为下限，多站点从"触发式（出现第二站才做）"转为 **M3 计划内主线**；「是否对外产品」仍开放。
  2. **实施顺序不变**：仍是先用 dgcrane 跑通 M0–M2、再泛化多站（02 章教训"第 2 个实例才是检验"——不提前建多站骨架，但今天起一切命名与结构决定不得绑死 dgcrane）。
  3. **仓库命名去品牌化**：改荐 **`site-factory`**（备选 `mdforge`）；介绍文案更新为：`内容驱动的静态站生产系统：AI 把资料烧成 Markdown 真相源，确定性引擎渲染静态站，可见即可编辑，S3/CloudFront 发布。多站点、多语言；dgcrane 为首个试点站。`
  4. 多语言注记：dgcrane 的英文老站将来也可作为一个语言镜像迁入本系统（12 章机制通用），不限于中文站配翻译。

- **2026-07-03 ⑬ 开跑前实况盘点（用户质询触发："架构都好了吗/相关产品做了吗/要重写模版吗/两条路/为啥先 SEO"）**：逐行对代码核查，发现并修正蓝图三处与实况的漂移（行为权威原则，蓝图已回改）：
  1. **single-girder 仍用 legacy `product.html`**——蓝图 03 章登记表误记为超集。纠正之外把"切超集+退役"从 P2 **提前到 B1 搬迁之前**（双模版留到批量期 = 每次版式改动双份维护）；`overhead-cranes-for-sale.html` 澄清为 geom 基准素材、保留不删。
  2. **超集模版未用 `data-array`/`data-edit`**（repeat 绑定靠 `REPEAT_FM_ARRAY` 注册表、编辑类型靠形态推断）——04 章加现状注记：词汇表是目标态，注册表搬家仍 P2。
  3. **related-products 区残留旧 spec 词汇**（`data-dynamic` 等）——实为构建期静态渲染，台账#8，随 SEO 顺手清。
  **相关产品功能本体确认已实现**：4 个产品页 MD 全有 `related_products` 数据、模版有区、静态渲染、geom 背书；posts 骨架无相关产品区（待对照原站确认是否本就没有）。
  **两条生产路答复**：路 2（AI 烧 MD→引擎结构注入→HTML）为唯一生产路，7 页全走它；路 1（AI 烧 MD→AI 直出 HTML）历史上存在（multi-point 即由它生成）、实证收敛后封存（DECISIONS ①）——其"AI 自由设计版式"的能力由"每篇一份骨架模版"承接，通道可按用户指令恢复。
  **SEO 排序确认**：非拍脑袋——14 章 G3 门禁规定搬迁入口=SEO+图片闸门+钩子就位（先搬后补=每页返工），且 SEO 一步顺带清偿面包屑硬编码与 related 旧词汇两笔模版债。

- **2026-07-03 ⑭ 用户授予流程自主权 + 最终执行流程定稿 + SEO 边界重划**：用户明示"不看旧拍板，按你自己的想法和流程来，之前的可以抛弃"。落地三件：
  1. **最终执行流程定稿于 [EXECUTION.md](EXECUTION.md)**（阶段 0–4：地基✅ → 管线强化 → 搬迁准备 → 批量搬迁页循环 → 收尾待命），含贯穿纪律与用户四个介入点。关键设计：发布本就延后 ⇒ **搬迁全程草稿态自主推进，无需逐页/逐批等审**；只有危险操作、红线（AWS/真实发布）、真阻塞三种事中断等人。
  2. **SEO 边界重划为四层责任制**（08 章 §5 重写，01 章非目标行同步修订）：L1 技术层（构建期自动）/ L2 页面语义层（烧页时做）/ L3 关键词策略层（AI 真实调研起草，排在批量搬迁前）归 AI；L4 站外层（外链/公关/预算）不做；上线后监测分析归 AI（用户给 Search Console 数据）。07-01 的"②层全不做"边界作废。
  3. **面包屑 JSON-LD 先两级**（首页 > 当前页）：中间分类页尚未迁入，三级 trail 会指向死链；B3 分类页迁入后升三级。og 资产绝对 URL 前缀收敛为单常量，随 07 章 base-path 拍定复核。

- **2026-07-03 ⑮ 全链路逻辑走查（15 章）**——用户指令"先理论、可改可删、必须亲走一遍"。8 条链×三问法（从哪来/谁有权改/改了谁知道），实证 7 项发现：
  **F1 真相源分裂（最重）**：title/description/lang 只存在于 `build.mjs::pages[]` 代码里，slug/template 双处登记两套格式——**运营改不了页面标题**（明明是 L1 值）。修复=登记表由 content 目录派生、元数据入 frontmatter、**删手写数组**（build.mjs:17 原注释的兑现）；连锁治愈：管理=目录、多语言登记、SEO 单源。
  **F2 验证双源 + 覆盖 2/7**：verify-geom 自带注册表与 compose，**7 页只有 2 页受 geom 保护**。修复=单源 import + 基准成页面字段 + 覆盖 7/7。
  **F3 读写注册表不对称行=债务地图**：components/crane-types/related-products 三行读写路径不同（related 真相是 `seed`，渲染吃派生的 `items`）；完成判据=两表逐行相同后整表删除（P2）。
  **F4 面包屑结构 5 份拷贝**：1.2 方案改为抽 fragment 一次治五处（与询盘表单同病同治）。
  **F5 基准祝圣流程**（06 章已补）：新基准=人审通过的渲染快照独立显式提交。
  **F6 传播矩阵补第十行"新增页面"**（12 章已补）：新页→旧页的反向核对。
  **F7 post 骨架复制权衡**：确认 body-only（无 chrome 重复），族内共同块随篇复制为已接受代价，公共块逐个 fragment 化缓解。
  执行影响：阶段 1 重排，新增 **1.0 真相源收敛**先于一切（含 SEO 重做——前次实现已主动回退，设计经 check 验证过）。全部修复方向是**删与并**，修完系统更简单。走查通过判据："一个页面=一份 MD，系统里不存在第二个需要人工同步的登记处"。

- **2026-07-03 ⑯ 阶段 1.0 真相源收敛落地（走查 F1/F2 的治疗）**：
  1. **`page:` 登记块成为规范**（04 章 §1 已更新）：必填 slug/type/lang/title/description/template，可选 mode/geomBaseline/family；旧的顶层 `slug:`/`template:` 双份行删除（grep 证实无代码读取，族@版本语义保留为 `family:`）。
  2. **build.mjs 手写 pages[] 数组删除**，登记由 `loadPages()` 扫 `src/content/*.md` 派生，缺登记/缺字段抛错中止；**verify-geom 第二份注册表删除**，改为 `import { pages }` 过滤 `geomBaseline` 派生。
  3. **验收**：改造前后 7/7 页 dist 产物 **逐字节一致**（shasum 全量比对）+ 单测 3/3 + geom 两页 1:1——重构零行为变化的数学证明。
  4. 未尽项：**1.0b** 其余 5 页按祝圣流程冻结基准后纳入 geom（覆盖 7/7）；verify-geom 的 compose 确认为**功能性差异**保留（A 面须保留 marker 供 [data-block-id] 测量，生产 compose 剥 marker），坑账#4 的同步责任缩小到这一处。
  5. **提请用户**（CLAUDE.md 归你维护，我不改）：CLAUDE.md §5 "MD 自带 slug 与 template"一句已被 `page:` 登记块取代，方便时可自行更新。

- **2026-07-03 ⑰ 定调：缺的是"贯通主轴"，云端产品不是"未来"而是本体（用户强烈反馈）**。用户明确：不要再分析已有文档、要一条从 MD 到访客到多语言到管理的完整系统流程；并补两条硬需求：**MD→HTML 要"AI 能组装、代码也能组装"两条路并存**；**抛弃 Python**（预览/验证换 Node）。承认此前 altitude 错误：把 S3 存储/Lambda/管理软件/云端可见即可编辑/本地同步全推成"M2/M3 触发式未来"，而这些正是用户要的本体。**动作**：
  1. 新增 [00-系统总流程](00-系统总流程.md) 作为**主轴**（README 置顶必读）：按用户列的顺序 ①原料→MD ②MD→HTML(引擎/AI 两路) ③SEO ④多语言+发布(S3 三平面+CloudFront) ⑤访问 ⑥可见即可编辑(云端往返: browser→API→edit-save Lambda→patchMarkdown→草稿→预览→publish) ⑦同步其他语言(i18n-diff+translate) ⑧管理软件(SPA+Cognito+API+DynamoDB) 逐段落地，含 S3 桶/路径规范 + 9 个 Lambda 清单 + 现有内核如何原样打包成 Lambda 载荷。
  2. **B 路（AI 组装 HTML）正式恢复**为"新页族/特殊版式"的合法组装器（此前封存），铁律=产物带同套 marker、过 validateBlocks+geom，故 AI 组的页也能被引擎重渲、被编辑器编辑。
  3. **抛弃 Python** 进执行队列。
  4. 存储真相模型钉定：git=源真相+版本；S3 三平面=drafts(草稿)/web(发布,CF源)/media(媒体)；DynamoDB=非真相的运营元数据(任务/锁/缓存)，页面列表从 content 目录派生（呼应 F1）。
  5. 红线不变：纸上设计不碰真实 AWS 账号，建资源须用户明示授权。

- **2026-07-03 ⑱ 装配契约成文 + 引擎/站点分离设计（回应用户两问：引擎能否服务下一个站？AI 生成 HTML 须守什么规则？）**：
  1. 新增 **[16 装配契约 v1](16-装配契约.md)**：把 render/editor/patchMarkdown 的实际行为规范化为站点无关的三方契约——MD 规范（A）、marker 封闭词汇表（B）、命名铁律（C）、结构硬约束 D1–D6、**装配十律**（E，AI 组页的全部规则）、**机器合规判定 C1–C4**（F，validateBlocks/坐标可达/同形收敛/geom——合规是跑出来的不是声称的）、版本化规则（G，契约先行、引擎跟随，禁止私开后门）。
  2. **"AI 能组装"的正式语义拍定**：AI 负责**结构的创作**（新页族造超集模版 / 特殊版式造每页骨架），引擎负责**值的装配**；AI 直写 dist 仅为须特批的逃生舱。两路产物同构（同形律），故 SEO/发布/编辑/翻译不关心页面谁组的。
  3. 新增 **[17 引擎与站点分离](17-引擎与站点分离.md)**：可机器验证的复用判据（**第二个站的 PR 里引擎目录 diff=空**）；四层切分（引擎/契约/站点/运行）；`site.config` 设计（站点常量单点登记，同 F1 思想）；**引擎纯化清单 5 项**（grep 实证：build.mjs:22-23 域名、build.mjs:73 "首页"、render.mjs:35/42 两注册表、面包屑 trail——全是"配置放错位置"，无逻辑分支，搬家即纯）；多站目录布局（engine/ + sites/<site>/），迁移时机=v1.0 冻结后或第二站启动时（不在搬迁中途动）；**新站 9 步剧本**（每步标注引擎改动=0）。
  4. 00 章升级为**详细设计版**：①–⑧ 每步展开【输入→输出/谁干/详细流程/失败回退/验收门】，publish 原子 8 步、编辑往返 8 步（含锁与冲突检查）、i18n 字段级增量 6 步算法成文。
  5. 本轮纯设计，零代码改动（用户指令"先不要动代码，先有全部流程"）。

- **2026-07-03 ⑲ 地基化第一步落地：引擎字符串入口 + 编辑链路回归门（用户"最好变成地基后续复用"）**：
  1. **`render.mjs` 抽出 `renderBodyFromString(template, mdString, opts, srcLabel)`**——纯函数、不碰文件系统；原 `renderBodyFromMarkdown(tmpl, path, opts)` = `readFile` + 委托。**证明**：改造前后 `dist` 全部 HTML/XML 逐字节一致（`git stash` 前后 shasum 全等）。**意义**：这正是云端 `edit-save`/`render-page` Lambda 需要的形态——它们从 S3/git 拿到的 MD 是**字符串**而非路径（呼应 00 章⑥"内核纯函数原样当 Lambda 载荷"，此前 render 只有吃路径的入口，是隐性缺口，现补上）。
  2. **`scripts/edit-probe.mjs` 常驻回归门**（进 `npm run check` → pre-push + CI）：把 455 个可编辑坐标逐一真实写回→重渲→验证；**纯内存**（用字符串入口 + `applyPatch`/`applyArrayOp` 纯函数，读盘只读一次、绝不写真实文件，进程崩溃也不脏工作区）。三类探针：token 写回 428 过、identity 无损往返 302/302、重复组增删 36/36。
  3. **棘轮基线**：27 个已知未修缺陷（1.8 name 黑洞 26 + p 槽拆裂 1）登记进 `KNOWN_BROKEN`，判据"只能变好不能变坏"——非基线坐标失败→退出码 1；基线坐标被修好→提示摘除。**从此"编辑器能否准确改任何地方"是 CI 守住的属性，不靠人肉抽查。**
  4. 这是"变地基"的正确第一步：先立**证明编辑链路不退化的常驻网**，再在网下做 1.8 修复与 E-3 引擎纯化（拆单站耦合）——顺序是"先网后拆"，每步 dist 逐字节 + geom + 探针三重兜底。

- **2026-07-03 ⑳ 多语言同步机制拍板：编辑事件驱动的字段版本戳为主线，git 差异引擎降为兜底（用户明确"git 先保留但[编辑事件流程]为主线"）**：
  1. **背景**：用户不接受"多语言同步依赖 git 取历史版本"当主路。追问下澄清出更好的模型——**同步的信号不必事后算，编辑时就已产生**：编辑器写回本就带字段坐标（`overview`/`hero.title`…），那一刻"这篇、这个字段、变了"即确定。
  2. **主线机制（不依赖 git）**：源 MD 每个可翻译字段带**版本戳** `i18n_rev.<字段>`，编辑器改哪个字段就 +1 哪个；目标语言 MD 记 `translated_rev.<字段>`。**过期 = 目标戳 < 源戳**——两个当前 MD 里的数字一比即知，不取历史、零数据库、不碰 git。改一段只 +1 一个戳，故"待更新清单"天然精确到字段。
  3. **用户批准的五步流程为主线**：改中文某段 → 源该字段 +戳 → 各目标语言该字段判过期（后台显示"EN:overview 待更新"）→ translate 只重译该字段写入目标 MD 同字段（其余含人工润色一律不碰）+ 抬 `translated_rev` → 引擎重渲该语言该文章 HTML → 发布。结果：目标页那一段对上、整页别处不动。
  4. **人工润色保护天然成立**：英文手改不动源戳；中文没再编辑→目标戳==源戳→判已同步→翻译永不碰。冲突检测用目标侧 `manual` 标记，不依赖 git。
  5. **git 的位置**：仅当有人**绕过编辑器**直接改源（无编辑事件、没 +戳）时，作为可选兜底做一次字段级 diff 补清单。故 git 保留但非主线。
  6. 落章：12 章 §3.1/§3.2/§3.3/§3.4/§3.5 全部改写为版本戳主线；00 章 ⑦ 同步更新。实施仍属 M3（多语言启用时），现只钉死机制不返工；`i18n_rev`/`translated_rev` 字段名即预留。

---

- **2026-07-04 ㉑ 多语言完备性审计：登记未决缺口 U-1~U-5（用户"后续再说，但需要记下来"）**。用户追问"多语言这块够了吗"，按 [13 章六透镜](13-完备性审计协议.md)复核，除已定项（镜像树 §3.1、编辑事件版本戳 §3.3、多语言关联清单/manifest）外，发现五处该定未定，**登记为 OPEN、不擅自当决策落解法**：**U-1 不翻译字段**（型号/品牌/代码/电话/单位——现设计每字段都送译，AI 会译坏型号品牌）、**U-2 术语表 glossary**（部件/类型固定译法，否则同物多名、SEO 散）、**U-3 部分翻译态显示策略**（未翻字段藏/占位/中文——中英混排一页伤 SEO，须用户拍板）、U-4 两道校验（hreflang 双向对称 + 结构镜像一致，进 check）、U-5 RTL/locale 格式化（显式划范围外）。对标 WPML，U-1~U-3 正是成熟方案的必需件。落 12 章 §3.9 + 本清单 #7–#9。**关联清单/manifest**（把一逻辑页的各语言 MD/HTML 路径+状态+过期字段聚合成一条派生记录，供管理软件查）已在对话中提出、用户认可为"缺的那个关联关系"，**已正式并入 [12 章 §3.1.1](12-修改传播与多语言同步.md)**（含 JSON 形状；非真相、从 site.config 声明 + 内容目录/版本戳派生，呼应 F1 与 00 章⑧）。2026-07-04 补记：此前只在本日志留了一句"待并入"，会导致新 session 读正文看不全——已落正文修正。

- **2026-07-04 ㉒ SEO 系统性架构总览成文 + 修正过时现状（用户"该先做 SEO 系统架构描述，不然下次就不对了"）**：
  1. **动因**：对话暴露 SEO 缺一份"系统视图"——散在 08 章各节、且 §1"现状缺口"是 1.1 前旧状态（写"canonical/OG/JSON-LD 一个都没有"），**新 session 读到会误判什么都没做**。这正是用户担心的"关 session 就做歪"。
  2. **08 章新增 §0「SEO 架构总览」**（置顶、下次接手先读）：六问答完整架构——① 每页 SEO 记录在哪（=MD `page` 块字段，单一真相 F1）② 字段→标签派生映射表（seoHead，build 时）③ 多语言 SEO（hreflang 自动派生不手设、每语言 canonical、切换器同源、site.config 管 base/langs）④ 编辑与后台（今天改 MD/未来 SEO 面板；hreflang 后台只调输入不手打）⑤ 四层责任 ⑥ **现状三态矩阵**（✅已建/🔶待建/📐设计未建/⏸暂缓，evidence-based）。
  3. **修正 08 章 §1 过时内容**：标注 canonical/OG/JSON-LD/sitemap 已于 1.1 建成（7 测试守），仍缺=面包屑 RDFa（1.2）/图片（1.3），暂不做=robots（共存期）/hreflang（M3）。
  4. **两个后台组件落名**：00 章⑧ 管理界面清单补 **SEO 面板**（写回 MD `page` 块）；hreflang 后台"只调输入不手打"写进 08 §0④。均属管理软件层、待产品化。
  5. 纯文档、零代码。此后 SEO 有单一权威架构视图，接手者读 08 §0 即得全貌与真实现状。

- **2026-07-04 ㉕ 图片存储方向：当前本地、既定未来外链（模式2）——细节后续制定**（用户"肯定是要外链的，不是本地"，及"这个后续制定吧，目前先本地"）：
  1. **拍定方向**：图片终态走**外链**——媒体上传到对象存储/CDN，页面/MD 引用**上传后的 URL/key**，不进 git（模式2）。**成立条件（必记约束）：媒体键不可变**（同 key 永不覆盖，换图=新 key），以此保住"老版本 MD 的图 URL 永远解析、内容+图原子可复现"——加了这条，外链不再破坏 git 复现性，且对多站×多语言更优（仓库不被二进制撑大、CDN、同图跨语言共享）。
  2. **压缩归位**：压缩是**上传边界**的事（`media-ingest` 云函数，sharp），**不是**永久本地脚本——废除原 1.3 的 `npm run img` 常驻管道设想。
  3. **过渡与节奏**：**目前先本地**（模式1：图在 `public/assets/img/`、相对路径，现状不动）；外链是**既定未来**，但**方案细节（`MEDIA_BASE` 进 site.config、不可变键实现、云管道到位前 URL 指老站媒体还是新桶）后续制定**——本条只钉方向，不落实现（且新桶碰 AWS 红线，须用户明确点头）。
  4. **反证据/收敛**：渲染器 `resolveImg` 已放行完整 URL（裸文件名才加 `IMG_BASE`），故未来 MD 存完整 URL 近乎零改；1.3 降级为"仅本地搬迁前对存量超标图一次性压缩"（EXECUTION 已改）。
  5. **蓝图待理平**：08/05/11 章现有"压缩后落 `public/assets/img/` 并 commit"（模式1）与 00 章 `写 sf-media`（模式2）本自相矛盾——**未来制定外链方案时一并扶正为"当前本地过渡 / 终态外链"**，此前不擅改（避免把"当前=本地"误写成"已外链"）。

- **2026-07-04 ㉔ 新增 `data-required` marker（必填校验）+ 重申 ⑭.3（面包屑 JSON-LD 先两级）**（用户做完 1.2 后"再看看还有什么问题"催出）：
  1. **动因**：1.2 走查发现——MD 若漏写 `breadcrumb` 块，引擎**静默出模版占位「首页 > 当前页」上线**（实测复现），违反 fail-fast 铁律（2026-06-26 决策：改坏即报错、不静默退默认）。普查 7 页所有"占位符原样上线"字段，结论：**绝大多数良性**（shared chrome 如品牌语/CTA/`inquiry_form.title`，或 MD 值恰等于默认的巧合），真正"退占位即错"的只有面包屑一类。
  2. **`data-required` marker**（与 `data-optional` 互为反义，登记进 [16 章](16-装配契约.md)标记表 + 装配十律#5）：挂 `data-field`→值须解析到非空；挂 `data-repeat` 容器→数组须非空；缺则 `render.mjs::validateRequired` **抛错中止**。判据交给造模版的人：占位是"真能上线的默认"（fallback 图/shared 语）→不挂；是"假文字"（`当前页`）→挂。已挂 `breadcrumb.current`/`trail`、h1 `title`。**§2 干净**（marker 驱动、不认字段名）、**通用**（后续任何必填字段复用）。**AI 烧 MD 的规则**由此从"靠记得"变"忘了 build 红"。
  3. **纠 1.2 的 ⑭.3 越界**：1.2 曾把面包屑 JSON-LD 由两级改成读 trail 出 N 级——但中段分类 URL 本系统未构建=死链，**结构化数据不该断言 404**（正是 ⑭.3 当初压两级的理由）。退回两级（首页项取 `trail[0]` 与可见同源），可见面包屑仍数据化 N 级（其中段死链是既有问题、随 B3 分类页迁入解）。B3 后 JSON-LD 再升三级。
  4. 验证：负例（删 breadcrumb/title 的 MD）现报错；正例 build + check 全绿（probe 483、geom 7 页 1:1、单测 7/7）。

- **2026-07-04 ㉓ 补齐 SEO 对话决策入蓝图（用户"都写进系统架构了吗，能放心让你执行吗"）**：核对发现 §0 架构总览在册，但近几轮三块只在对话、换 session 会丢——补进 08 章：**(A)** §0④ 配置分两类逻辑（站点级共享→`site.config` 一处；每页专有→留 MD；方便靠**聚合视图**不靠搬家，同 12 章 manifest）；**(B)** §7「SEO 未决清单」SEO-1~SEO-4（含 2026-07-04 拿代码自我核对的更正：JSON-LD 富摘要字段是"推荐非必需"、且 post 的 MD **根本没有日期/作者数据**，补 JSON-LD 须先改内容模型）；**(C)** §0⑦ 程序化执行逻辑（SEO 无单独按钮、是发布的自动副产品、`seoHead` 原样进 publish/render 云函数）。**能否放心执行的结论**：技术层（canonical/OG/JSON-LD/sitemap/hreflang）确定性、有测试门兜底、可自主执行；但"放心"建立在"记录完整 + 验证门 + 用户可自核"上——故先补全记录再执行。SEO-1~4 登记进下方待拍板 #10-11。纯文档零代码。

- **2026-07-04 ㉖ 多语言同步系统化补强：复用判据 / 审计链（能力接口制）/ 目标侧结构禁改 / 上线闸（用户"好好系统设计——其他站模版可复用这套逻辑代码、可见即可编辑同步、有迹可循、有对应信息"）**：
  1. **本轮对话先后提出的"内容哈希水位线 / 差分重烧"方案，经与 ⑳ 复核后放弃**——⑳ 的编辑事件版本戳已覆盖同一需求且是用户拍板主线（哈希须事后计算，版本戳在按保存的瞬间免费产生；粒度同为字段级；"差分重烧"=⑳ 五步流程第 4 步本身）。**记录在案，防止未来 session 再发明第三套。** 同理撤回本轮"现有 MD 平移进 zh/ 目录"提议——12 章镜像树（源在根=权威位、目标在 `<lang>/`）已定且更优：零迁移、"源的权威性"结构可见、站点无关（根=source 而非=中文，`sourceLang` 由 site.config 声明）。
  2. **12 章新增 §3.0 设计总则（复用判据）**：多语言在四层（引擎/契约/站点/内容）的归位表 + 三条硬判据——①引擎 grep 不到语言代号/字段名/域名；②一切状态=纯函数 `f(内容目录, site.config)→manifest` 可重算，CLI check 与后台 API 共用同一内核（⑲ 字符串入口同思路）；③ **editor.js 零改动：+戳在保存链路服务端**，编辑器不知道多语言存在。"**可翻译坐标系=可编辑坐标系**"成文。
  3. **12 章新增 §3.10 审计与追溯，且当轮即被用户纠偏一次**：初稿把痕迹全写成 git commit，用户"后续哪有那么多 git？必须用 git 吗？"——与 ⑳ 同型教训在审计维度复发。改为**能力接口制**：审计只依赖 ①事件记录（保存请求天然带 谁/何时/坐标/新值——信号编辑瞬间产生，本地由 git commit 免费承载，产品期=append-only 事件日志，**不必是 git**）② 版本化存储（本地=git，云端=S3 版本化或 git 远端，随待拍板 #3 定）。此为**第四条缝：历史缝（git ↔ 事件日志+版本化存储）**。**日常同步主线零依赖这两接口**（只读当前 MD 的戳）；追溯判据本身不含"git"字样，换提供者判据原样成立。
  4. **目标侧结构编辑禁止**入 §3.6：结构增删只在源语言页做，目标页编辑器只开值编辑（否则镜像失锚）；"按市场定制内容"显式登记为将来另拍的新命题，不预开口子。
  5. **U-4 由两道扩为四道校验**：原 hreflang 对称 + 结构镜像（含 `translated_rev` 键 ⊆ 源 `i18n_rev` 键），新增 ③孤儿镜像检测（目标 MD 无源→红）④ per-lang fragments 骨架同构断言（剥文本比标签树——每语言一套 chrome 是新引入的漂移面，坑账#4 同病类，须有门）。
  6. **§3.7 新增 `deployLangs` 上线闸**：site.config 声明对外语言（现 `["zh"]`），未列入语言可建/可预览/不部署不进 sitemap+hreflang——"机制就绪"与"对外"一个配置位解耦；**en 对外=整站切换日**（老站英文活在域名根路径，提前上 /en/ = 同域两套英文互撞），与 robots.txt 接管同刻。
  7. 纯文档零代码；实施节奏仍守 §3.8（现只钉机制与预留字段，M3 启用落地）。"对应的信息"需求由既有 §3.1.1 manifest 承接（㉑ 已并入正文），不另造。

- **2026-07-04 ㉗ 重大纠偏：git 只是代码仓，从来不是内容真相（用户："从来没说 git 当源，我只是用来存代码的；后面所有的东西肯定都在 AWS 上，没有 git"）**：
  1. **纠什么**：⑰.4 曾记"存储真相模型钉定：git=源真相+版本"、待拍板 #3 曾记"澄清已给：git 存源不存网站"——**均属错记/过度引申，即日作废**。正确模型：**git=代码仓（引擎/脚本的开发与 CI/CD），仅此而已；产品期一切运行时资产（内容 MD、模版/fragments/site.config、媒体、发布产物）都在 AWS，内容路径上没有 git。**
  2. **AWS 原生真相模型**（把旧"git 当数据库"的职能逐项还位）：**版本/回滚**=S3 版本化（内容桶开版本控制）；**作者/审计**=append-only 事件日志（DynamoDB；⑳/㉖ 洞察：谁/何时/坐标/新值在保存请求里天然齐全，顺手落一条即可）；**发布快照**（原 git tag）=发布记录对象（该次发布所含各页内容版本 ID 清单——快照=版本 ID 集合）；**草稿**=sf-drafts（本就是 S3）；**兜底字段 diff**（原 git show）=按版本 ID 取 S3 旧对象做结构化比对；**多文件原子手术**（跨语言 key rename 等，原"同一 commit 内完成"）=S3 无跨对象事务，由操作层任务串行化+完成标记实现——**这是唯一需要自建的能力，登记为 M1 设计项**。
  3. **本地 M0 现状重新定性**：`src/content/` 在仓库里 = **开发期过渡便利，不是架构承诺**；P0"建 git 远端并推送"仍有效（备份开发仓=代码+过渡期内容），但不再承载"真相层上云"含义。多语言机制不受影响——⑳ 版本戳本就设计为"只读当前 MD、零 git"，戳在 MD 里、MD 搬到 S3 戳跟着走，**一字不改**。
  4. **理平清单（grep 实证的"git 当真相"渗透点；本轮已改 12 章+待拍板表，其余待用户点头后一次系统性扶正）**：00 章 L112/122/132/157/161/194/196（"git 源真相"/tag 发布/"状态机=git 投影"）、03 章 L10（"真相层=git 仓库"）、07 章 L11（"release=git tag、git 就是状态机"）、09 章 L74/87、10 章 L15/51/65/93（M1①"git 远端成为唯一权威"）、11 章 L19/32/41/45/92/135（"托管 git 每站一仓库"、"git 当数据库"）、14 章 L11（改表述为代码仓备份）。
  5. **12 章即时修正 4 处**：§3.0 内容层"随 MD 进 git"→"戳在 MD 里，MD 在哪戳在哪"；§3.1 `source_commit`→`source_version`（本地=commit，产品期=S3 版本 ID）；§3.3 兜底 `git show`→版本化存储接口；§3.10 产品期提供者=S3 版本化拍定（删"或 git 远端"）。
  6. 红线原样：以上全为纸上设计，**不建任何 AWS 资源**。

- **2026-07-04 ㉘ 多语言一页垂直切片试点落地（用户"我还是想看看多语言的生成"；14 章纪律：提前必须显式，此为显式提前，非偷跑 M3）**：
  1. **范围**：5-ton 文章页一对（zh 源 + en 镜像），链条=烧 en MD → 登记扫语言子目录 → build 出 `/en/` 页 → hreflang 成对 + og:locale → 版本戳 → `i18n:status` 状态报告。**明确不做**：不上线（本地 dist 而已）、编辑器不自动加戳（保存链路服务端加戳属 M3）、语言切换器未接、en 页外壳暂中文 chrome、U-1/U-2 未拍板故翻译时人工保守处理（DGCRANE/LD/LH/QD/HD/FEM/CD/MD 型号一律不译）。
  2. **落地形态（全在装配层 build.mjs + 内容，引擎 render.mjs/editor.js 零改动——§3.0 判据实证）**：①镜像树=12 章 §3.1（`src/content/en/<同名>.md`，配对键=镜像文件名 `i18nKey`，`raw/` 排除）；②目标语言页 **slug 自带 `<lang>/` 前缀**（URL 即路径：`en/posts/5-ton-overhead-crane`）——registry 唯一性、dist 输出路径、edit-server 路由三处零改动白拿，源语言仍走 /zh/ base（现行 dist→/zh/ 拓扑），M1 拓扑翻转（dist→域名根）时统一；③ `pageUrl(page)` 按 langDir 派生 URL；④ hreflang 组内互指 + **x-default=源语言页**（12 章 §3.7 原文：指默认语言 zh）+ og:locale/og:locale:alternate 按映射；⑤ **deployLangs 闸以常量落地**（sitemap/hreflang 只认闸内语言；**试点期全开 `["zh-CN","en"]` 以便本地看整链，上线前收回 `["zh-CN"]`**，终态归 site.config）；⑥ 版本戳按 ⑳ 手埋初值（zh `i18n_rev` 22 字段 / en `i18n.translated_rev` 镜像），`scripts/i18n-status.mjs`=manifest 最小内核（纯函数只读当前 MD 戳，CLI 与未来后台 API 同内核）。
  3. **验收实录**：单测 8/8（新增 hreflang 双向对称+单语言页不输出 hreflang 断言=U-4 ①道先声）；probe **533/533**（en 页 +25 坐标全绿——**英文润色开箱即用**，编辑器对 en MD 直接可写回）；geom 两基准页 1:1；hreflang 仅出现在 5-ton 两页（其余 zh 页无漂移）；stale 演示实测：`i18n_rev.types` 1→2 → 状态报告即显 "en: 1 个字段待更新 → types"，复原即回"已同步"。
  4. **试点暴露的已知债（登记不装死）**：D-1 生产 sitemap 目前会随闸全开收 en URL（上线前随闸收回即消）；D-2 zh/en 双页 dist 拓扑不对称（zh 在根、en 在 `en/`），M1 部署拓扑翻转时统一为 `<lang>/` 对称；D-3 en 页 chrome（导航/页脚/表单体）仍中文，fragments 拆语言目录属启用期第一步（12 章 §3.8）；D-4 语言切换器仍是 header 里的死快照，接真链接与 hreflang 同源（12 章 §3.7）留启用期。
  5. 试点不改变节奏共识：主线仍是 EXECUTION 阶段 1 收尾 → 阶段 2 搬迁盘点;多语言批量启用仍等英文站拍板 + U-1~U-3 拍板。

## 待拍板清单（阻塞项，按顺序）

| # | 问题 | 状态 | 阻塞什么 |
|---|---|---|---|
| 1 | 托管与发布 | **已拍定 S3+CloudFront**；实施经用户明示**暂缓**（只写脚本骨架不建资源，红线继续） | M0 发布线（暂缓中） |
| 2 | 询盘表单 | **已拍定 Postmark 路线**；实物暂缓，**先 mock**（端点常量，切真改一行） | 上线前换真端点 |
| 3 | **代码仓**托管选型（GitHub / Gitee / 内部）——范围经 ㉗ 修正：**仅代码**（引擎/脚本的开发与 CI/CD）；**内容真相与 git 无关**（产品期=S3 版本化+事件日志） | 待选（旧记"git 存源不存网站"系错记，㉗ 作废） | P0 远端备份（开发仓）+ M1 CI/CD；**不再卡"真相层上云"** |
| 4 | 对内工具还是对外产品 | **半决（⑫）**：对内多站已确认为主线；是否对外仍开放 | M3 |
| 5 | 生成 worker 的 LLM 通道 | 待验证（M2 前） | M2 |
| 6 | 发布授权档（最严 / 批量 / 分级） | **暂按最严运行**，随时可改 | 不阻塞，只影响节奏 |
| 7 | **多语言·不翻译字段（型号/品牌/代码/电话/单位锁翻译）** | **RESOLVED 2026-07-07**：分两层——①整字段=推导可翻译集合（text/rich∪SEO白名单−image/link），image/link 天然排除，`i18n-validate.mjs` 校验名册≡推导（先校验后推导）；②行内保护词=站点层 `terms` 表 lock 段+emit 注入+lint（`lock-lost` 门禁）。CLAUDE.md 决策日志 2026-07-07 | 已落地（阶段A/B） |
| 8 | **多语言·术语表 glossary（部件/类型固定译法）** | **RESOLVED 2026-07-07（07-08 简化）**：与 U-1 行内保护词合成一个站点层 `src/i18n/terms.<src>.<tgt>.yaml`（lock+map）。`--emit` 把相关术语注入翻译提示交翻译节点（AI）遵守——**纯提示词**。07-08 删除初版 lint 层（`i18n-lint.mjs`/`lintField`/`--lint`）：对能读指令的 AI 属过度设计（每次都过、逐字校验误报率高），日后换笨 MT 再加回。CLAUDE.md 决策日志 2026-07-07/08 | 已落地（阶段A，07-08 精简） |
| 9 | **多语言·部分翻译态显示策略（未翻字段：藏/占位/中文）** | **RESOLVED 2026-07-07**：= 上次英文 + 发布门禁。过期字段照渲上次英文（渲染层零改动）；有「从未翻译」字段的目标页发布门禁暂扣、不进 dist/sitemap/hreflang（`build.mjs::isPublishable`）；源新增段用 `--sync-structure` 灌源占位保镜像完整。**否决蓝图原"藏掉"倾向**（会藏有效旧译）。CLAUDE.md 决策日志 2026-07-07 | 已落地（阶段C） |
| 10 | **SEO·JSON-LD 深度（Article 日期/作者、Product 价格）+ 内容模型加日期字段** | OPEN（2026-07-04；根子是 MD 缺字段，非仅生成器） | 08 章 §7 SEO-1；富摘要非必需，不阻塞上线 |
| 11 | **SEO·每页 noindex 开关 + 首页级结构化数据** | OPEN（2026-07-04） | 08 章 §7 SEO-2/3；noindex 随首功能页、Org 随首页 |
