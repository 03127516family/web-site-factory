# 需求定稿 v2 — 内容驱动静态建站系统（复审稿）

> **用法**：逐节审，标"改"；§7 待定项逐条拍。拍板后摘入 CLAUDE.md 决策日志。
> **范围**：本场重构讨论的全部结论（2026-07-29 ~ 08-03）。你想到的和你没想到的都在——没想到的在条目标注 ★。
> **铁规**：本文件只是需求，不是开工令。实现需你另行发话。

---

## 1. 目标与边界

- **R1 目标**：建立一个全新的内容驱动静态建站系统——页面保真、**非技术编辑零学习维护内容**、确定性装配、能上线。
- **R2 建立新系统**：从零设计新架构，不继承 WordPress、不迁移/还原任何既有站点；替代 WordPress 是动机，不是迁移。
- **R3 后议区**（你说"先实现，后谈；这不是你关心的"）：域名策略、301、全量迁移、切换/回滚、ICP、发布运维、编辑环境上云、认证、监控、培训、统计代码平移。见 §8。
- **R4 设计目标**：内容驱动 + 代码确定性装配，非技术编辑零学习维护内容。
- **R5 你的工作流不变**：你只出三样——文章、产品名、slug；AI 全包到 1:1。

## 2. 存储：JSON 取代 MD（核心决策）

| # | 需求 | 说明 |
|---|---|---|
| R6 | **结构化数据是唯一真相**：每页一个 JSON，落盘进 git，含 `"version": 1` | MD 退役；version 是未来格式演进锚点 ★ |
| R7 | **body 富文本 = 语义 type 树**（你提出的思路，验证为最优） | 编辑写回 `getJSON()` 直写，**零转换、无损**——旧系统 htmlToMd bug 工厂连存在余地都没有 |
| R8 | **type 是语义，不是外观** | 有界节点集（约 13 种：doc/paragraph/heading/text/bold/italic/link/bulletList/orderedList/listItem/table/tableRow/tableHeader/tableCell/image）。**无**颜色/字号/布局类型 |
| R9 | **JSON 不管外观，模版管外观** | 判别尺："换个风格的站，这信息还有意义吗？"有意义=语义进 JSON；无意义=外观归模版/CSS |
| R10 | 重复区 = 数组；加一行减一行 = 数组增删项 | specs/表格行/组件/案例同 |
| R11 | **schema 定义 + 写时校验**，写坏当场拒收 | 烂数据进不了盘 ★ |
| R12 | JSON→markdown **单向导出器**（给外部看内容） | 只导出不进仓，防双源真相 ★ |
| R13 | 存储格式决策**可逆** | body 备选 B=HTML 字符串；格式关在读/写端点里，换格式=一次性脚本 |

## 3. 渲染：JSON → HTML

| # | 需求 | 说明 |
|---|---|---|
| R14 | 引擎 **Astro**（替代手搓 render.mjs/build.mjs/static-server/edit-server） | 量产件，修 bug 是别人的事 |
| R15 | 渲染器 = **JSON 遍历器**（约 50 行） | 站点 class 约定集中一处（表格统一套 `.custom_tables`）；**未知节点 throw**——外观锁执法 |
| R16 | 模版 = **超集组件按数据裁剪** | 可选段：内容里有就渲染、没有就不存在——**"不是标题就不硬凑"是机制不是自觉** |
| R17 | chrome = 共享组件；询盘表单/面包屑/语言切换 = fragment 组件 | 现有 fragments 平移 |
| R18 | 产物：干净静态 HTML，零 `data-*`、零框架运行时 JS | 同现行标准 |
| R19 | **AI 烧 JSON 理性判断**：这段是不是标题/概述/规格，由内容说了算 | 烧完 schema 自带验收 ★ |

## 4. 编辑：直接修改（主用户 = 非技术同事）

| # | 需求 | 说明 |
|---|---|---|
| R20 | **页面即编辑器**：点字打字（光标落点击处）、点图换图、＋/− 增删行、正文回车加段 | 零学习成本 |
| R21 | **界面零污染**：无侧栏、无框群；悬停淡高亮；富文本可选小浮条（B/I/链接/列表，按钮可配） | 否决 Tina 式虚线框群 |
| R22 | **改的是结构化数据，不是 HTML** | 页面是界面/投影；HTML 永远不被编辑，由 build 从 JSON 重新生成——"直接改 HTML"物理上不存在 |
| R23 | **防呆四件**：schema 校验、会话内撤销、删行确认、保存前改动可见 | 非技术用户 ⇒ 硬指标 ★ |
| R24 | **图片全生命周期**：点图换图、上传新图（必过压缩闸门）、改 alt | 压缩闸门还老账（3-4MB 图拖 LCP）★ |
| R25 | **编辑器改不了外观**：只挂内容槽（带坐标元素），布局容器无坐标；节点类型里也无外观 | 物理锁死，不靠规矩 |
| R26 | **结构变化不进编辑器**：加段/新版式/新页 → 找你 → 你一句话给 AI | "内容编辑 + 网管"分工 |
| R27 | **"简单"三堆分工**：小修小补=编辑器；结构变化=一句话给 AI；批量全局=AI/脚本直达存储 | 批量双通道：AI 现有 + 人工界面缓建，架构不挡 ★ |
| R28 | 编辑入口分阶段：你机器 → 局域网 URL（同事用）→ 云编辑（后议） | 他们不可能敲 npm ★ |

## 5. 状态与工作流

| # | 需求 | 说明 |
|---|---|---|
| R29 | **内容有状态**：`draft` / `published`（枚举留口） | 你拍板"肯定有不同状态" |
| R30 | **保存 ≠ 发布**；build 只出 `published` | 复用现行 isPublishable 模式 |
| R31 | 保存两档：**存草稿 / 发布** | — |
| R32 | 回滚：「还原到已发布版」一个动作；git 全量历史兜底 | ★ |
| R33 | **草稿预览**：保存草稿后能以真实页面形态看效果 | ★ |
| R34 | **查找定位**：改之前能搜到字段在哪页 | 几百页时代的前置需求 ★ |

## 6. 多语言：一源多镜像 + 族谱

| # | 需求 | 说明 |
|---|---|---|
| R35 | **zh 源唯一真相，其它语言 = 同一骨架不同文字** | 镜像结构完全一致，只 text 节点值不同 |
| R36 | **三层联系**：①身份（稳定 pageId + locale 文件）②结构（镜像随源同步，不许单独改）③字段（指纹追踪新旧） | 沿用戳/指纹思想，改读 JSON |
| R37 | **族谱 `registry.json`**：中央登记处（pageId → 各语言 slug/status） | 代码维护、**可全量重建**——是索引不是第二真相（你提的"父亲知道孩子"） |
| R38 | **族谱驱动六件事**：结构同步 / 发布门禁 / 语言切换器+hreflang / 待译清单 / 新语言上户口 / 编辑器语言页签 | — |
| R39 | 孩子文件只带"姓"（pageId+locale）；指纹表 `i18n_fp` 管字段级新旧 | 各自单一真相，不双写 |
| R40 | **翻译回路四站**：寻找（代码）→ 翻译（AI）→ 写回（代码）→ 抬戳（代码）；术语表注入提示 | AI 只占"翻译"一站 |
| R41 | **body 树翻译**：按路径抽 text 节点给 AI，树结构代码保留——译文不爆骨架 | JSON 化红利 |
| R42 | **新语言 onboarding**：配置加语言 → 克隆骨架(draft/未译) → 门禁挡 → 回路批量 → 译完转 published → 自动出现在 /xx/、sitemap、切换器 | 数据驱动，零额外代码 |
| R43 | 过期照显示旧译（不藏）；未翻完不发布；**改 zh 自动标 en 待译** | U-3 沿用 ★ |
| R44 | 允许直接编辑镜像文字（=人工翻译，保存即抬戳） | — |
| R45 | 语言范围：按 zh+en 设计，架构不挡 34 语言 | 终态后议 |

## 7. SEO、功能、云（沿用/分阶段）

| # | 需求 | 说明 |
|---|---|---|
| R46 | SEO 技术层带走：canonical / OG / JSON-LD / sitemap / hreflang | 已实现，逻辑平移 |
| R47 | 站内搜索、首页、产品目录、案例列表、语言切换器带走 | 已实现，平移 |
| R48 | robots.txt 共存期不生成 | 现行决策 |
| R49 | 表单页面接线（/api/inquiry、/api/subscribe）；**真后端冻结待令** | 无你明确指示不碰 AWS |
| R50 | 部署分阶段：dist→S3 托管；素材 bucket；询盘→Lambda→S3（冻结） | 后议区联动 |
| R51 | 内容 JSON：本地 git 仓 + sync S3 先行；纯云化终态待定 | — |
| R52 | **提醒**：aws-cn 无 CloudFront，海外加速另配 | ★ |
| R53 | 杂页族清单（about/contact/FAQ/视频/PDF）登记在册，随用随迁 | ★ |

## 8. 验证与实施方式

| # | 需求 | 说明 |
|---|---|---|
| R54 | **绞杀式**：旧系统冻结，新系统旁边建，逐页搬逐页验 | 不大爆炸 |
| R55 | **POC 先行**，达标再谈下一步；随时可撤 | — |
| R56 | 验收三件套：verify:geom 比对 / 编辑自动化验收（17 条）/ schema 校验 | 客观不靠嘴 |
| R57 | geom 定位：**工程工具**（验证搬过来的页没走样），不再是业务硬指标 | 新站逻辑下的降级 |
| R58 | **新页生产流程**：裸文章 → AI 烧 JSON → schema 验 → 族谱注册 → geom 验 → 发布 | P5 的完整展开 ★ |
| R59 | POC 验收四条件：**V1** geom 1:1；**V2** 编辑自动化（含写回 JSON 正确）；**V3** 状态门（draft 不出/published 出）；**V4** schema 执法（写坏拒收） | — |

## 9. 拍板记录（2026-08-07 拍定 ✅）

| # | 决策 | 拍定 |
|---|---|---|
| T1 | body 存法 | **C=语义 type 树**（"选择语义化标签也就是格式化"；POC 已全程实证） |
| T2 | post 页族自由版式 | **每篇一个独立布局组件（倾向），随第一篇 post 迁移时最终落定**（"先独立吧后续再说"） |
| T3 | 内容云化程度 | **本地 git 仓先行，终态 S3**（"以后都是放 S3，目前都在本地即可"） |
| T4 | 编辑批量人工界面 | **缓建**（"不着急"——AI 通道先够用，架构不挡） |

## 10. 旧系统资产处置

- **带走**：9 页内容 + en 镜像（转 JSON）；全部 HTML 设计（模版→组件）；CSS/JS/img 素材；verify:geom；i18n 戳/指纹思想、terms 表；SEO 头逻辑；搜索索引；表单 fragment + form-submit.js。
- **退役**：render.mjs / build.mjs / edit-server.mjs / editor.js / md-write.mjs（htmlToMd、patchMarkdown）；名字注册表；CLAUDE.md 半本 doctrine（marker 词汇表/对称铁律/坐标系/§2.4/§9）。
- **改造**：i18n 脚本族（改读 JSON）；pages 登记表 → Astro 路由 + collection。

---

> ★ = 你没明确提出、由我方补充的条目（复审重点）。

---

## 11. 增补讨论结论（2026-08-07，POC-3/4 通过后）

> 来源：「手里只有一个 HTML 怎么处理」「HTML 能不能当真相」「模版会不会非常多」三连讨论的拍定。

### D1 HTML 的定位：原料 + 证人，不是零件，更不是真相

- **HTML 当真相 = 视图当真相（WordPress 死法）**：① 改内容 = 逆向工程（等于重建刚炸掉的 htmlToMd bug 工厂）；② 换风格 = 全量重写（外观糊在每个文件里）；③ 无法复用（sitemap/相关产品/逐字段翻译/搜索索引都抠不出数据）；④ 无法校验（同族页面结构漂移无 schema 管）。
- **手里只有 HTML 的处理流**：AI 通读 → 烧 JSON（schema 验，烧错拒收）→ 拿原 HTML 当 geom 基准验 1:1 → JSON 成为真相，HTML 退场。
- **HTML 的最大价值是当证人**：geom 逐元素拿它当 ground truth（POC-4 已实跑：旧 dist 页当基准，78 块全对齐）。
- **唯一例外**：永不改/不换风格/不翻译的页，可直接静态 serve（共存期旧 dist 页即如此）。

### D2 HTML → 组件是稀有一次性事件：先判族

- 版式**见过**（已有族：产品/文章/分类…）→ **不动组件**，只抽内容烧 JSON，已有组件渲染。
- 版式**没见过**（全新族）→ AI 烧一次组件（旧 §9 七步判断逻辑平移到 .astro 编写：切块→判形→命名→抽模子→复 chrome→验 geom）；此后该族第 2~N 页只烧 JSON。
- 非开发人员三条路都不碰代码：新页给文章 / 改内容页面上改 / 搬迁给 HTML 文件。

### D3 模版数量纪律（摁住"模版爆炸"）

- **组件跟版式族长，不跟页面长**：页面可成千上万，组件个位数~十几个封顶（产品/文章/分类/首页/杂页族）。
- **组织法**：族级分文件；段级拆小组件（Hero/Gallery/SpecsSection…各自小而单一）；页级只是装配清单。
- 族内差异小 → 条件裁剪（`{j.x && …}`）；差异大 → 拆页级清单文件（段组件照样复用，重复近零）。
- **唯一禁止**：按"页"出模版（300 产品 300 文件 = 死路，2026-06-25 已否决过一次）。
- 模版组织法与地基解耦：JSON 树 / walker / schema / geom 不因选超集或拆清单而变。
