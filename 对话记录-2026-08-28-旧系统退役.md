# 对话记录-2026-08-28-旧系统退役

## 背景

用户两连问：①「列一下从内容变成界面到修改到多语言到 SEO 经过哪些文件」②「感觉现在非常复杂，系统看不懂，也不知道对不对，只能通过结果看，过程都不知道」。

回答中拆出复杂度三笔账：
1. **过渡期假账**：新旧两套系统并存（旧 MD 系统 8-07 已进退役倒计时但没执行），认知负担翻倍。
2. **文档形态不对**：决策日志 30+ 条是流水账，没有一张给人看的架构导览。
3. **「不知道对不对」一半是分工（机器只枪毙 100% 判得对的，内容质量必须人看结果），一半是真缺口**（无一键全矩阵命令）。

用户拍板（原话浓缩）：①搜索做完整，然后旧系统直接删；②先补再删；③历史文档（对话记录/demo/design/docs）不动。

## 决策

- 补齐旧 dist 有而 site 没有的 6 样：free-standing 中文页（调查中新发现的迁移漏洞，en 镜像孤儿）、404、搜索、首页、产品目录、案例列表。
- 断三绳：geom（快照化）、CI（切 site 矩阵）、pre-push（改 site 检查）。
- 删尸：scripts/ src/ dist/ public/ .i18n-events.jsonl。
- 文档：CLAUDE.md 重写 + site/docs/architecture.md（先行落盘）+ 本篇。

计划：`docs/superpowers/plans/2026-08-28-legacy-retirement.md`（12 任务）。

## 执行摘要（commit 链）

| 任务 | commit | 结果 |
|---|---|---|
| 1 补迁 free-standing zh 源 | e89befa | md-to-json 直通 + template 字段修正为 ProductPage；zh/en 双页入 dist |
| 2 composeStatic + 验收骨架 | 33e10a9 | 先红后立 |
| 3 404 页 | 22fe63a | /zh/→/ 按 D1 修正 |
| 4 站内搜索 | 17f82a9 | search-index 16 条 + 搜索页 + 双产物链接线 |
| 5 首页+列表页+sitemap | dbbb70b | accept-derived 10/10 |
| 6 geom 快照化 | 1db66fd | 10 页基准固化；geom-post 并入 geom-check |
| 7 CI 切换 | 72c4b90 + 7dbe318 | 首跑红在 8092 端到端（见偏差 3），加固后复跑 |
| 8 pre-push | 64ac147 | check + geom |
| 9 根 package.json 瘦身 | f3e69db | 代理壳 + prepare 保留 |
| 10 删除旧系统 | 1bd6fe0 | -10136 行，删后全矩阵绿 |

## 执行期偏差记录（相对计划）

1. **geom 快照首跑抓出三个旧有偏差**（非本次引入，一并修复）：
   - `kernel.mjs::productionJson` 调 `projectPage` 没传 `existingTrail` → 生产构建的 en 页面包屑漏中文（镜像 JSON 里的手写英文 trail 被源树克隆盖掉）。修法：投影前从镜像文件取回 trail 传入。
   - 6 个 en 镜像的 `breadcrumb.trail` 数据是中文（trail 是 U-1 结构排除字段、不走翻译流水线，从没被本地化）：全部英文化（Home/Cases/Eot Cranes/Overhead Cranes/Jib Cranes，URL 指 /en/ 旧站地址）。
   - 5 个 post kit 面包屑 JSX 的 `{' '}` 与旧模版空白不同 → `.current` 右移 4px。改成分隔符前置（`{i > 0 && ' '}`）+ 删除 current 前空格，与旧字节同构。
2. **en/5-ton 案例段 27px 换行差**：TM 机翻措辞（"5-ton Double Girder Overhead Crane"）与旧手写 en MD（"5 ton double girder"）不同，多折一行。按 2026-08-18「en 内容真相归 TM」决策，该页基准以当前生产渲染重新固化（`geom-baseline` 文件带 `source` 字段自描述；zh 页基准永远来自旧 dist）。若要恢复旧手写措辞：8092 编辑页改那句英文 → TM 记人稿，永不再犯。
3. **CI 首跑红在「8092 端到端」**：本地同命令绿。疑似 poc5 已知的 `#edlToggle` 偶发超时在 CI 慢机器放大（2026-08-24 有记录）。日志下载需 admin 权限拿不到，步骤加固为：8092 不就绪→倒 edit-server 日志；poc5 失败→自动重试一次。复跑结果见 git/CI。
4. **计划外修补**：`serveStatic` 路径穿越闸门对相对路径目录误伤（`join('./dist',…)` 不以 `./dist` 开头→403）——geom-check 内部传绝对路径不受影响，只影响临时调试脚本，记此备查（如复用 geom-lib 须传绝对路径）。
5. 执行中两次误在仓库根跑 `npm run build`（shell cwd 不持久），把旧 dist 重建了两次——旧 dist 是旧引擎从已提交 MD 的确定性产物，重建无害；随后已整体删除。

## 残留

- 部署未接（无 deploy workflow）。
- en 产品页（4 个镜像）是旧 dist 没有的新页，无几何基准对照旧站——breadcrumb 等已按惯例英文化。
- GitHub API 本次会话多次连接重置（本地网络），CI 状态轮询受阻。
