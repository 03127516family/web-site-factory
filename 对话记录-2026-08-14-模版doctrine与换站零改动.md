# 对话记录 2026-08-14：模版 doctrine 拍定 + spec B2（一 astro 一模版）+ 换站零改动

> 上半场（spec B 初版）见 git log `8f5c44b…8620b96`；本篇记下半场：用户驳回我的「不 kit 化」判断、
> 拍定 doctrine、给出换站验收标准，我自治实施 S1-S7 全程。commit `b24e03a…83c95af`。

## 一、拍定（用户）

1. **驳回**：我主张「旧 4 篇槽位文章专属、补 meta 也烧不了新文章，故不 kit 化」——用户：模版化是我的
   决定权，你管他能不能公共形态；一个页面我想变成模版我就变，大不了自己套自己。
2. **doctrine 四条**：一 astro 一模版／自己套自己／通用件不吞并／对不上就空着（按需渲染）。
   与 WordPress 模版模型同构（Template Name 头→下拉一项，逐页选；已查证官方文档）。
3. **换站验收标准**：现在只有 dgcrane；换一个站，什么都不用改，只加模版就能用。
4. **自治授权**：自己讨论、自己检查、自己实现、自己继续，不等「继续」。

## 二、实施（7 提交，accept-burn 最终 123/123）

| 步 | commit | 内容 |
|---|---|---|
| S1 | b24e03a | meta 路径声明（section.titlePath / text.path）+「族:套件名」指定烧制 |
| S2 | fe0de55 | **引擎去站化**：chrome 值全从套件 example 克隆（后缀机械替换/壳克隆防泄漏/related seed 清空）；提示词示例自 example；SITE_ASSETS 常量；stripHtml 定性迁移插件。引擎 grep 仅剩迁移注释 |
| S3 | 3ba37ef | **chrome 站内化**：5 个 chrome 文件拷进 site/src/chrome/（旧系统照用），build 逐字节一致 |
| S4/S5 | 8972ec4 | **kit-init** 一条命令升格 + 旧 4 篇成可选套件（配对 12/8/10/8 与人工推导逐项一致） |
| S6 | 83c95af | 控制台 optgroup 列全 6 套件 |

**换站自检**（"这样就够了吗"的自辩补洞）：发现 Chrome.astro/套件还读旧仓 `../src/fragments`——
只拷 site/ 去新站会炸 → S3 补上。剩余清单见 spec §7.5（SITE_ASSETS 一处常量、i18n 配置簇待隔壁
落地、控制台静态选项待 edit-server 落地接扫描、4 篇扁平件待收单源）。

## 三、执行期实证（教训）

1. **canonical 大小写 bug**：`'posts'.replace(/s$/,'')+'Page'`='postPage'≠'PostPage'，auto 判族
   静默落到字典序第一个套件（32t）——症状是"字段不在白名单"，根因在套件选错。修：首字母大写
   + 收成 burn-lib::canonicalKitOf 单一实现。
2. **kit-init import 深度正则 bug**：`(x)+` 只捕获最后一次重复，`../../` 两层原样未动（发现于
   生成件复核）。修：整段捕获 `(?:\.\.\/)+`。
3. **assemble 派发器漏传 example**：内层签名改了、转发没改。症状=chromeOf 报缺 example。
4. **find 回调变量泄漏**：`find(c => …)` 之外又用 `c`（exampleSnippet），运行时 ReferenceError。
5. 烟测里我 mock 编了 gantry 不存在的 `price` 槽被白名单拒——引擎正确，测试预期错。
6. 4 个套件 astro 是工作区版本拷贝（含隔壁 T9 守卫：h2/h3 与段体同守）——守卫正是「缺席即裁」
   的前提；隔壁落地后删扁平件收单源。

## 四、用户测法（浏览器）

http://localhost:8092/__burn → 下拉可见 6 套件；贴文章选「龙门选购页版式」烧 → 报告看 fieldMap/
未用段 → 存草稿 → 编辑器精修链接。产品路照旧。

## 五、下一步候选

en 路由 template 派发（等隔壁）；控制台接 scanKits 动态渲染（同）；重复区富文本编辑器写回真机验；
i18n 配置外置 site.config（同）；第二个站的实装验证（真正的换站验收）。
