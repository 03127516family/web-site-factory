# 沟通记录 · 确定性渲染器 + 可见即可编辑(WYSIWYG)+ 增删重复项 + 缺口评估

> 日期：2026-06-24
> 承接《对话记录-2026-06-23-差异化机制-overhead页-渲染器路线.md》。
> 组织：路线落定 → 产出 → 现状 → **缺口评估**，不是逐字稿。
> 项目：`websitere-placement-system`（替代 WordPress 的内容驱动建站系统）

---

## 0. 接续点

上一份截止于「渲染器走代码还是 AI」未决。本轮把它拍了，并一路做到「可见即可编辑 + 增删」，最后做了一次诚实的缺口评估。

---

## 1. 路线落定：「两个都要」

不是二选一，而是**分工**：
- **AI 负责创作**：原料 → 结构化 MD（frontmatter + markdown 正文）+ 个别不规整片段。产出落盘成持久工件，烧一次。
- **代码负责装配**：MD → 填字段 / 克隆重复 / 裁可选 → 拼页。确定性、每次 build 免费、保证 1:1。

本质 = 当初搁置的 Claude 路线（有 MD 中间层 + 确定性渲染），但 AI 不出局，钉在「创作」段。

---

## 2. 阶段 1：确定性渲染器 + 几何 1:1 铁证（commit `90be1ee` / `9c4d1ae`）

- **`scripts/render.mjs`**：纯代码渲染器。`data-field` 按点路径填（富文本/纯文本/列表/img-src/a-href）；`data-repeat` 按 MD 数组克隆首个单元；`data-optional` 无数据删整块；markdown 正文按 `## 标题 <!--block:KEY-->` 切块、`### 项`切重复，mdToHtml 转 HTML。
- **MD/模版抹平**：spec 收进 `spec:`、价格做富文本字段、正文加 block 锚、内嵌图带尺寸；cta 文字下移到 `<span>`、缩略图补 data-field。发现 MD 是「frontmatter + markdown 正文」混合体。
- **修了 4 个真 bug**：克隆组件图残留错误 srcset(会加载错图)、alt 卡首项、灯箱链接全指首图、installation 双层 `<p>`。
- **`npm run verify:geom`**（`scripts/verify-geom.mjs`）：CDP 驱动系统 Chrome，逐个 `[data-block-id]` 元素比包围盒，确认「渲染器产出 vs 静态模版」**逐元素像素对齐**。single-girder 实测零差异。**源码层(内容/结构) + 几何层(像素) 双验证。**
- CDP 端口用 9456，避开 Edge/Chrome 默认 9222 冲突。

---

## 3. 可见即可编辑（WYSIWYG，Path A）（commit `aec31dd`）

在**渲染好的页面上原位改内容值**（文字/图片/跳转链接），自动写回 MD。定的是 **Path A 全 WYSIWYG**（富正文也直接在页面改，含 HTML→md 反解）。

**核心洞察**：`data-field` 既是渲染器填充靶，又是编辑器 DOM↔MD 绑定 —— 一套标注三用。**结构只许存在 MD，模版固定、不可编辑。**

**契约（三方共用）**：
- `data-md` 写回坐标：`fm:<点路径>` / `mdhead:<block>` / `mdbody:<block>[#<i>]`
- `data-edit` 编辑类型：`text` / `rich` / `image` / `link`（值含行内标签 → rich，价格 `<span>` 行靠此正确识别）
- `POST /api/save {slug, coord, kind, value}` → patch `src/content/<slug>.md`

**产出**：`md-write.mjs`（按坐标 patch，yaml Document + `lineWidth:0` 保持 flow 单行/diff 干净；htmlToMd 反解 + 净化 contenteditable 脏输出）、`editor.js`/`editor.css`（扫 `[data-md]` 挂交互）、`edit-server.mjs`（现渲染编辑模式页 + 注入 + /api/save）。`npm run edit` → localhost:8081。生产 build 零 data-*。
**实施方式**：钉死契约后 **3 个 agent 并行**（渲染器编辑模式 / 写回+htmlToMd / 前端），我集成。

---

## 4. 增删重复项（v2 结构编辑，通用）（commit `bb00c7f`）

用户提出增删（举例规格行，但明确是「打比方，要通用」）。

**原则（不跑偏）**：增删 = 改 MD 数组、模版一行不动、reload 重渲。渲染器本就按数组长度克隆。加 = 塞空占位项 → 用现有改值填；删 = splice。
- 编辑模式给可增删组容器打 `data-group`、单元打 `data-idx`；`POST /api/array-op {slug, group, op, index}`。
- `md-write.arrayOp`：splice frontmatter 数组（NEW_ITEM 占位默认值）；**components/crane-types 双处同步**——frontmatter 数组与 markdown `###` 项按同一 index 增删。
- 前端每组「＋添加」、每项 hover「×删除」(二次确认) → array-op → 保滚动 reload。
- **规格行改造样板**：`spec{命名字段}` → `specs[{text}]` 数组(data-repeat)，价格 `<span>` 作富文本项保留。示范「固定块改造成可增删」。
- 过程中修：introduction 正文被外部污染(`### 标题`被截成`###`，12px 漂移，查清 htmlToMd 本身正确)、端口残留旧 server 致 405、老测试引用失效路径。

---

## 5. 编辑器补全 + 污染修复（commit `29e8f05`）

用户实测提出两条 + 踩到一个 bug：
1. **链接内文字/图片不能编辑**（CTA「报价要求」、相关产品标题/简介/图、案例标题都嵌在 `<a>` 里，点击被跳转劫持）→ 编辑模式中和锚点跳转、可编辑子元素 stopPropagation 抢占；改 URL 仍在卡片非编辑区触发。
2. **swiper 画廊增删挪到缩略图条**（大图区被模版写了 `pointer-events:none` + swiper fade + overflow 裁剪，不适合放控件）→ 渲染器把结构标记从 `gallery` 改到 `gallery-thumbs`(映射同一 gallery 数组)；＋添加条提到 `.ltgallery` 外避免被裁；缩略图 ×/改图 stopPropagation 不导航。
3. **🐛 徽标污染内容（用户实测发现）**：规格行 `<li>` 既是 `data-idx` 单元又是 `data-md` 字段，× 徽标被 append 进可编辑 `<li>` → 存盘混入 `<button class=editor-remove>×</button>` 并累积。**修法**：`currentValue` 存盘前克隆并剔除 `.editor-*` 控件；徽标设 `contenteditable=false`。CDP 实测净化后值干净。

---

## 6. 引擎通用性（用户问「其他文件能直接复用吗」）

- **通用部分**（写一次全页共享）：`editor.js`/`css`（只认 data-md/data-group/data-edit）、`render.mjs`、`md-write.mjs`、`edit-server.mjs`。single-girder 与 overhead 已共用，无一行专为单梁。
- **「符合要求」= 4 条**：① 有 MD 数据源(写回靶) ② 模版带 markers ③ 登记进 `pages` ④ 其重复组/字段在已知映射表(`REPEAT_FM_ARRAY`/`classifyField`)里。
- **同类页(再来一个产品)≈ 免费复用**；**结构全新的页**要扩那几张映射表(小而有界)。
- **离「即插即用」只差一步**：MD frontmatter 已自带 `slug`/`template` 声明 → 把 `pages` 改成自动扫 `src/content/*.md` 即可「扔个符合约定的 MD 就自动可编辑」。**未做。**
- 现成反例：overhead 页模版合规但**无 MD 数据源** → 编辑服务显示「不可编辑」。补个 MD 即可。

当前可增删组：`gallery, gallery-thumbs, specs, components, production-flow, crane-types, cases, related-products`。

---

## 7. ⚠ 缺口评估（诚实版，用户要求记录）

总判断：**对「本地、单人、内容微调」够用且扎实；离「交给非技术同事日常用的成熟 CMS」还有明显距离。**

### A. 功能缺口（最可能很快碍事）
1. **富正文编辑太裸**——overview/introduction 是裸 contenteditable，无工具栏(加粗/列表/小标题/插链接要懂 HTML)，contenteditable 本身脏(回车生成 div/br、粘贴带样式)。**最大功能短板。**
2. **不能重排**——只能增/删，不能拖动/上下移。画廊、案例顺序有意义。
3. **图片只能填路径**——无上传、无素材库、无存在性校验；新增项空 src = 破图。

### B. 健壮性 / 安全（给真人用就暴露）
4. **即时保存，无撤销、无校验**——失焦直接写 MD，删错/清空/乱填立刻落盘，后悔药只有 git；值编辑无二次确认、无必填/格式约束。
5. **无权限、无并发控制**——谁连上端口都能改，两人同时编辑会互相覆盖。单人本地 OK，联机不行。
6. **增删整页 reload**——保了滚动但有闪烁、焦点丢失。

### C. 验证盲区（过程缺口，需坦白）
7. **交互一直靠人手点验证**——渲染/接口/几何/存盘净化能自动验，但**真实点击**(锚点/swiper/对话框/contenteditable 手感)没法全自动测。**徽标污染 bug 就是这么漏过去的**，可能还有别的交互 bug 潜伏。要么搭 CDP 点击级自动测，要么多依赖人工复测。

### D. 结构隐患（暂不疼，埋着）
8. **组件/型号「双处结构」**——名字同时在 frontmatter 与 markdown `###`，改名只更新一处，另一处陈旧(不影响渲染)。味道。
9. **htmlToMd 往返边界**——复杂富文本(表格/嵌套列表/粘贴内容)可能接不住而漂移。
10. **段级操作缺失**——不能开关/增删/重排**整个段落区块**(data-optional 段)，只能段内改。

### 优先级建议
若继续投入：**①富文本工具栏 → ②重排 → ③图片上传/选图 → ④撤销/校验护栏**。安全/并发除非联机给别人用，否则可缓。
**「够不够」最终取决于：给谁用、用到什么程度**——自己/技术同事本地调内容≈现在够了(补富文本工具栏即很顺)；交非技术人当日常 CMS 则 A、B 基本都要补。**此问题待用户拍。**

---

## 8. 文件 / 命令速查

- **命令**：`npm run build`(生产，零 data-*) / `npm run edit`(编辑服务 localhost:8081) / `npm run verify:geom`(几何回归) / `node scripts/md-write.test.mjs`(写回自测)
- **引擎**：`scripts/render.mjs`(渲染+编辑模式标记) `scripts/md-write.mjs`(写回+arrayOp+htmlToMd) `scripts/edit-server.mjs`(/api/save、/api/array-op) `scripts/build.mjs`(导出 pages/composePage) `public/assets/js/editor.js` `public/assets/css/editor.css`
- **内容/模版**：`src/templates/product.html`(markers) `src/content/single-girder-eot-cranes.md`(数据源)
- **契约**：`data-md`(写回坐标) / `data-edit`(类型) / `data-group`+`data-idx`(增删) / `REPEAT_FM_ARRAY`(组→数组)

## 9. 本会话 commit（a63c12f 之后）

```
29e8f05 编辑器补全 — 链接内编辑 + swiper画廊增删；修复徽标污染
bb00c7f 增删重复项 — 通用结构编辑（+/× on data-repeat 组）
aec31dd 可见即可编辑 — WYSIWYG content editor (npm run edit)
9c4d1ae 几何回归验证器 (npm run verify:geom)
90be1ee 确定性 MD→HTML 渲染器
e1f4f33 docs: 2026-06-23 沟通记录
0956f9f / 7d219b2 / efd9dba  overhead页 + 超集 + 差异机制
```

## 10. 遗留 / 下一步

1. **拍：编辑器给谁用、做到什么程度**（决定补不补缺口 A/B，见 §7）。
2. 缺口按优先级补：富文本工具栏 / 重排 / 图片上传 / 撤销护栏。
3. （可选）pages 自动扫描 `src/content/*.md` → 即插即用可编辑。
4. （可选）overhead 补 MD 数据源，作「第二个产品直接复用」实证。
5. （可选）CDP 点击级交互自动测，填补验证盲区。
6. 其余产品(6–14)抽样，确认无别的新模块类型。

---

*本记录截止于「编辑器缺口评估完成、待定使用场景与优先级」。后续另起记录。*
