# 沟通记录 · 真相源决策 + DGCRANE 产品页 1:1 还原

> 日期：2026-06-22 ~ 06-23
> 用途：本次会话的交接/沟通记录，供后续其他人或 agent 接手。按"决策 → 产出 → 排错根因 → 当前状态 → 下一步"组织，不是逐字稿。
> 项目：`websitere-placement-system`（替代 WordPress 的内容驱动建站系统）

---

## 0. 一句话背景

要把现有 WordPress 站（dgcrane.com，起重机厂商）换成"内容驱动、AI 生成静态文件、部署 S3/CDN"的新系统。
有两份设计文档：**codex 版**（本项目在用）和 **Claude 版**（另一条路线）。本次先厘清路线分歧，再动手把目标站的一个产品页 1:1 本地还原、并改造成可被 MD 驱动的模版。

---

## 1. 真相源决策（最重要的结论）

### 1.1 两份文档的根本分歧只有一个：真相源是谁

| | codex 版（采用） | Claude 版（搁置） |
|---|---|---|
| 真相源（编辑/重发布依据） | **HTML 草稿** | blocks/schema |
| AI 产出 | 直接吐 HTML | 只填 blocks |
| 渲染 | 无渲染器，AI 即渲染 | 确定性渲染器把 blocks 算成 HTML |

### 1.2 关键概念澄清（讨论中反复确认）

- **访问态 ≠ 真相态**：两条路线访客拿到的都是 S3 上的**静态 HTML**，体验完全一样。blocks/schema 只躺在后台数据库给编辑器用，访客碰不到。
- **"有结构化中间层" ≠ WordPress**：界定 WordPress 的是"每次访问在活服务器现拼 HTML"。发布/构建时转一次、访客拿死文件（Hugo/Astro/SSG）= 与 WordPress 划清界限。三方对比：
  ```
  WordPress： DB → PHP 现场拼 → HTML    ← 每次访问、活服务器
  Claude版：  blocks → 渲染 → HTML       ← 发布时一次，之后死文件
  codex版：   AI → HTML                  ← 创建时一次，之后死文件
  ```
- **blocks 的优势只在两件"重新生成"的事上才回本**：① 全站换皮/改结构（改一处模板重渲染全站）；② 多语言字段级增量（改一句只重译一句）。**日常改文字/图片，两套完全等价。** "内容自由"和"全站统一可维护"物理对立，没有架构能同时拉满。

### 1.3 决策

- **先走 codex 路线：HTML 当真相源，不建 blocks 渲染器/双向编辑器。** 本站上线后不全站换皮、日常只改文字图片，blocks 的前期税回不了本。
- **blocks 方案保留为未来选项**（不废弃）。
- **唯一低成本保险（必须做）**：让 AI 产出的 HTML 强制带稳定标记 `data-template` / `data-field` / `data-block-id`。将来若要英文站，可按字段比对做增量翻译，省掉整套渲染器；万一未来要切 blocks，这些标记是反解析回结构的抓手。

> 一句话：**HTML 当真相源 + 标记不能省。标记近零成本，渲染器才是当前不该交的税。**

落档位置：
- 讨论纪要：`docs/superpowers/specs/2026-06-22-真相源决策与讨论纪要.md`
- 决策已并入 codex 设计文档：`docs/superpowers/specs/2026-06-19-ai-static-cms-design.md`（§3.6 真相源决策、§4.1 加 `data-block-id`、§13 多语言字段级增量、§6 唯一性校验）

---

## 2. 分支策略

- 新建 **`claude` 分支**，只保留 `docs/`（计划），移除 codex 的实现与资源（commit `16ea2cc`）。
- **`main` 分支完整保留可恢复。**
- 本次产品页还原工作就在 `claude` 分支上从零做。

---

## 3. 产品页 1:1 本地还原（核心产出）

目标页：`https://www.dgcrane.com/zh/products/single-girder-eot-cranes/`（单梁桥式起重机）。
要求：**1:1 还原外观 + 全部资源本地化 + body 做成可被 MD 驱动的模版**。不参考 codex 既有文件，直接以真实站为唯一来源。

### 3.1 做法

- 用 4 个并行 subagent 分别做：CSS/字体本地化、header fragment、footer fragment、body 模版。
- **CSS/JS 一律本地化原站的，不重写**（用户要求真 1:1）。
- 用无头 Chrome 截图 + CDP 几何对比验证。

### 3.2 产出文件

```
src/fragments/header.html      A·页头 1:1（多语言/mega-menu/社交/搜索）
src/fragments/footer.html      A·页脚 1:1（5列链接/联系/微信码/社交/版权）
src/templates/product.html     B·body 模版 1:1，11 段 section + 136 个 data-field
src/templates/product.contract.md   模版契约（见 §6）
src/content/single-girder-eot-cranes.md   参考实例内容源
src/layouts/document.html      站点外壳（复刻原站 #wrapper，按原序引本地 CSS）
scripts/build.mjs              组合注入 header/footer + 预留 MD 填充 hook
package.json                   build / serve 脚本
public/assets/                 全本地化资源：CSS · 字体 · cssimg · img · js
```

### 3.3 资源本地化结果

- 222 个资源引用**零缺失、零外链泄漏**（`wp-content`/`gstatic` 全本地化）。
- 仅 5 个 cssimg 是**原站本身就 500 的死链**，忠实保留原状（非待修项）。
- JS 全部本地化（见 §5）。

### 3.4 预览

```
npm run serve   # 构建并起服务，访问 /products/single-girder-eot-cranes/
```

---

## 4. 布局排错与根因（重要，避免重复踩坑）

还原后发现两处与原站不一致，用 **CDP 驱动真实 Chrome（非 Playwright）做逐段几何对比**定位，根因是同一类：

### 4.1 画廊撑满全宽（应为 1160 居中）

- 原因：subagent "把每段包成独立 `<section>`" 时，把画廊从 `#product.wrap`（1160 居中容器）里拎成了**全宽独立 section**，脱离约束 → 撑满 1440。
- 修复：画廊降级为 `<div>`，**嵌回 `#product` 内部**。几何回到 x=135 / w=1160，与原站一致。

### 4.2 生产流程九宫格竖排（应为 3 列）

- 表面：`.process .grid.demo-gallery` 应 `display:flex`（9 步 3 列），实际 block（竖排）。
- **真根因**：原站关键规则是 **`#product .process .grid {display:flex}`**——以 `#product` 开头。而修画廊时我把 `#product` 在画廊后就闭合了，导致后面整个正文脱离了 `#product` 作用域。
- **`main.css` 里有 89 条 `#product xxx` 作用域规则**（`.ltgallery`/`.pcon`/`.process`/`.craneinfo .pro-info`/`.types` 等全靠 `#product` 当祖先）。
- 修复：让 **`#product` 包裹整个正文**——原站它从规格一直包到 inquiry-form（在 1913 行闭合），`related-products` 在 `#product` 外（全宽）。一次修复连带恢复了全部 89 条规则。

### 4.3 经验教训（写给后来者）

- **要真 1:1，应保持原站 DOM 嵌套不动、只就地加标记**；"把每段包成 section"会重组 DOM、破坏依赖嵌套与 ID 作用域的布局。
- 验证用 **CDP 全页逐段几何 diff**（本地 vs 原站），比肉眼逐个找稳得多。最终 12 段全绿对齐。
- 另一个假阴性坑：画廊"切换失败"是测试问题——swiper thumbs 模块监听 **pointer 事件**，合成点击必须派发 `pointerdown/pointerup`，功能本身从未坏。

### 4.4 顺带修复

- `main.css` 被上一轮本地化损坏（截断约 1745 字节，殃及 `.process` 区）→ 从原始文件重做安全 url 改写、整体替换。

---

## 5. JS/效果来源说明（避免误解）

swiper 画廊、灯箱、折叠、复制等效果**全部用原站既有 JS，没有自己重写**：

| 效果 | 用的库/脚本 | 来源 |
|---|---|---|
| 画廊主图/缩略图切换 | Swiper (`swiper.min.js`) + 内联 init | 原站 |
| 点图放大灯箱 | PhotoSwipe (`photoswipe.min.js`) | 原站 |
| 选项/部件折叠 | `jquery.accordion.js` | 原站 |
| 页脚复制 | `jquery.copy.js` | 原站 |
| 流程编号/型号分栏/平滑滚动 | `page-init.js`（从原站内联脚本原样抽出） | 原站 |

我只写了"管道"：`document.html` 按依赖排 `<script>`、`build.mjs` 组合、把内联脚本搬进 `page-init.js`、改本地路径。
**坚持用原站那份确切版本**：因为缩略图是 `float:left` 静态网格 + Swiper 混搭的老写法，换新版 Swiper 很可能让混搭失效，行为就 1:1 不了。

---

## 6. L1 模版契约合理化（让 body 真正"能当模版"）

明确了 body 以后要当模版，做了 **L1 级合理化**：**视觉 1:1 一像素不动（类名/CSS 全不碰），只把"模版契约"显式化**。（更激进的 L2 结构重写未做，会有视觉漂移风险，且与"先走 HTML 真相源、别过度工程"相悖。）

做了：
- **内容三分类**（MD 只驱动 content）：`data-dynamic`=动态（表单/相关产品，运行时 API）；`data-kind="shared"`=全站固定（hero 企业数据、production-flow 标准工序）；其余默认=content。
- **结构契约**写进模版顶部注释：`#product` 必须包整段正文（89 条作用域规则），不可拆段/提前闭合。
- **抽出 7 个可重复块的单元模版** + 固化字段 schema → 全部写进 `src/templates/product.contract.md`。
- **去噪**：只删可证明无副作用的 `id="1"`；WP 残留（`wpforms-*` 等）暂留（删了有视觉漂移风险），在契约文档里标注。
- 验证：全页几何 diff 仍 12 段全绿，**零视觉变化**。

权威契约文档：`src/templates/product.contract.md`（取代旧 FIELD-MAP.md）。

---

## 7. 当前状态

- ✅ 真相源决策已定并落档。
- ✅ `claude` 分支：header/footer/body 1:1 还原 + 全资源本地化 + JS 修复 + 布局修复（12 段全绿）+ L1 契约。
- ✅ 可 `npm run serve` 预览，外观/交互与原站一致。

## 8. 遗留 / 下一步

- [ ] **实现 `build.mjs::fillTemplateFromMarkdown()`**：让 `src/content/*.md` 真正驱动模版，跑通"MD → 页面"闭环，并用几何 diff 验证仍 1:1。（临门一脚）
- [ ] 把本轮实现 **commit 到 `claude` 分支**（尚未 commit / push）。
- [ ] 逐个核对其它依赖 JS 的交互（移动端菜单、部件 accordion、页脚复制）。
- [ ] 可选：接入原站还加载的 6 个插件 CSS（表单/语言切换器细节），已下载未接入。
- [ ] 5 个原站 500 死链：忠实保留，非待修。

---

*本记录截止于"产品页 L1 契约"完成时；后续进展另起记录。*
