# 沟通记录 · 页面差异机制 + overhead 页产出 + 渲染器路线之争

> 日期：2026-06-23
> 用途：本次会话的交接/沟通记录，承接上一份《对话记录-2026-06-22-真相源决策与产品页1比1还原.md》。
> 组织：决策 → 产出 → 现状 → **未决问题**，不是逐字稿。
> 项目：`websitere-placement-system`（替代 WordPress 的内容驱动建站系统）

---

## 0. 接续点

上一份记录截止于"单梁产品页 1:1 还原 + L1 模版契约"。本轮处理的核心问题：
**模版怎么推广到别的产品——尤其是那些"整体很像、但多出本模版没有的模块"的产品**（如对比表、参数表）。
最后落到一个更根本的岔路：**MD→页面这一步，是确定性代码渲染，还是 AI 生成？**（未决，见 §6）

---

## 1. 页面差异机制：超集裁剪

### 1.1 结论
模版 = **所有可能模块的超集**。每个可变块带标记，渲染时**按 MD 里"数据在不在"裁剪**：有数据→渲染保留，无→删除整块。同一份超集裁出不同产品页。

### 1.2 两个标记
- `data-optional="<key>"`：结构固定、整段可有可无的块；MD 无该 key → 删整块。
- `data-slot="custom-*"`：结构各异、塞不进固定字段的独有内容（对比表/参数表）；MD 给一段 markdown → 渲染注入，无 → 删。

### 1.3 四档差异速查
| 差异 | 手段 |
|---|---|
| 值不同（文字/图） | `data-field` |
| 数量不同（重复块多少） | `data-repeat` |
| 整段可有可无 | `data-optional` |
| 独有不规整内容（表格…） | `data-slot` 自由槽 |
| 完全定制布局 | 逃生舱（该页 `truth: html` 手改） |

### 1.4 "兜底设计"讨论 → 暂不做
曾讨论：给"没有对应块"的自定义内容一个**兜底布局**（如左标题/右内容，复用模版设计语言）。
头脑风暴后判断：① 模版正文其实是"上下堆叠"，左右只用于顶部摘要；② 让内容"像模版"的多是排版token不是布局；
③ 真要兜底更应是"给AI的风格指南"而非又一坨渲染机制（YAGNI）。**结论：先不做兜底。**

### 1.5 落地（已做、未 commit）
- `src/templates/product.html`：6 个内容段加 `data-optional`（gallery/overview/introduction/components/crane_types/installation）。纯附加属性，单梁页仍 1:1。
- `src/templates/product.contract.md`：新增 §7「页面间差异机制」+ §8 渲染器裁剪规格。
- 注：曾往模版塞 `data-slot` 模拟自定义块，应用户要求**已撤回**（保留 data-optional + 契约文档）。

---

## 2. 前 5 个产品的"缺口"调研

抽样首页「设备/products」前 5：overhead、single-girder(基线)、double-girder、underslung、single-girder-gantry。
逐段（h3）对照现有模版，**只列模版缺的**：

| 产品 | 模版缺什么 |
|---|---|
| single-girder | 无（基线） |
| double-girder | 无（100% 覆盖，仅文字/数量不同） |
| underslung | 无（同上） |
| single-girder-gantry | 优势（独立段）；另缺简介/起重机类型属"子集"，data-optional 省略即可 |
| overhead | 优势、保护装置、**简要技术参数比较(表)**、**细节规格按要求提供(表)**、哪个更好 |

**关键结论**：前 5 个里，模版唯一缺的"新视觉"是**对比表/参数表**；其余"多出来的"都是**额外的标题正文段**（同 `pro-info` 外观，复用即可）。子集型产品靠 data-optional。

---

## 3. overhead-cranes-for-sale 页产出（路 1：每页一份 HTML）

按"忠于 HTML 真相源——复用模版 class/标记，把 overhead 写成它自己一页"产出。

### 3.1 产出文件
- `src/templates/overhead-cranes-for-sale.html`（497 行，16 段 + 画廊）。由 subagent 照 product.html 范式 + 原页内容写成。
- `public/assets/img/product/` 新增 overhead 自有图 46 张（唯 `Crane-electric-control-bo.jpg` 是原站 500 死图，忠实保留）。
- `public/assets/css/landing-page.css`：+1 条通用 `.custom_tables table/th/td` 边框样式（一处生效全站自定义表）。
- `scripts/build.mjs`：`pages` 加 overhead 条目。

### 3.2 额外 5 段（data-block-id / data-optional）
advantages、protection（pro-info+ul）；spec-compare、spec-detail（custom_tables+裸table）；which-better（pro-info+p）。两表去掉了原站 tg1/tg2 class，用裸 th/td + 通用 CSS。

### 3.3 验证
13 个 h3 段与原页逐段一致；结构契约（#product 包裹、related 在外、画廊在内）OK；7 项规格、19 张画廊图；零 wp-content/外链泄漏。**注：尚未跑 CDP 全页几何 diff（仅结构级验证）。**

### 3.4 暴露的代价
每产品一份 HTML = **标准段骨架在多文件里重复**（DRY 损失）。改标准段结构要改 N 份。这是"不交渲染器税"的代价。

---

## 4. 合并超集模版（路 3 的前半）

应"创建一个新模版，是他俩的结合"——新建 `src/templates/product-superset.html`（576 行）：
- = product.html 的 12 标准段（单梁样例内容）+ overhead 的 5 段额外（overhead 样例内容），5 段带 data-optional。
- 结构契约保留、标签平衡。
- **未加进 build 清单**：因为没有渲染器时直接 build 会渲出全部 17 段=大杂烩。

> 它的价值只有**配上渲染器按 MD 裁剪**才兑现 → 引出 §6。

---

## 5. 目标工作流 + "直接点击就能看"的坑

目标管线：`MD(数据源) → 渲染器(填字段+按 data-optional 裁剪) → 拼 header/body/footer 进外壳 → 输出 HTML`。

**痛点**：现在每次要 `npm run build` + 起服务器访问。希望"输出一个 html 双击就能看"。
**坑**：页面用**绝对路径** `/assets/...`；双击是 `file://` 打开，`/assets/` 指向硬盘根 → CSS/JS/图全 404。**这就是现在必须起服务器的原因。**
两条解法：
- **A 相对路径**：资源改相对（`../../assets/…`），输出 `html+assets 文件夹`，双击 index.html 即可，零服务器。轻。
- **B 全内联单文件**：CSS/JS 嵌入、图片 base64 内联，真·一个 .html 可双击/发人。重（可能数 MB）。
倾向先 A。（**未定**）

---

## 6. ⚠ 核心未决：渲染器是"代码"还是"AI"？

`fillTemplateFromMarkdown` **能用纯代码实现**（标记↔结构化MD = 查找/替换/克隆/删除，零 AI）。
但用户指出：**他原本设想的 MD→页面是 AI 驱动的**。这戳中本质：
- **纯代码渲染器 = 当初搁置的 Claude 路线**（MD 结构化中间层 + 确定性渲染）。
- **AI 直接吐 HTML = 当初拍板的 codex 路线**（无中间层、无渲染器）。

| | 代码渲染器(Claude路线) | AI 生成(codex路线) |
|---|---|---|
| MD→HTML | 几十行确定性代码 | AI 写 |
| 确定性/成本 | 每次一样、建一次后免费秒出 | 每次可能不同、每次烧一次 AI |
| 新花样(表/乱序) | 只会做标记允许的，新东西要先加标记/槽 | AI 直接写出来 |
| 代价 | 渲染器税 + 结构僵硬 | 不稳定、1:1 难保证 |

**和解设想**：AI 负责"创作"（产品资料→写成 MD / 写出独有片段），代码负责"组装"（MD→填模版→拼页）。
但要清醒：这个和解版**本质已是 Claude 路线**（有 MD 中间层 + 代码渲染），跟当初 codex（纯 AI 吐 HTML）不同。

**这是当前最该拍的板。** 它决定下一步是"写那几十行确定性渲染器"还是"搭 AI 生成管线"。**未决。**

---

## 7. 当前文件状态（均未 commit，最后提交仍是 a63c12f）

- `src/templates/product.html` — +6 data-optional（单梁仍 1:1）
- `src/templates/overhead-cranes-for-sale.html` — 新建，overhead 整页
- `src/templates/product-superset.html` — 新建，合并超集（未进 build）
- `src/templates/product.contract.md` — +§7 差异机制 / §8 裁剪规格
- `public/assets/css/landing-page.css` — +1 条 custom_tables table 样式
- `public/assets/img/product/*` — +overhead 图 46 张
- `scripts/build.mjs` — pages 加 overhead；**且 single-girder 已被改指向 `product-superset.html`**（用户改的）→ 在渲染器落地前，build 单梁会渲出超集大杂烩，需注意。

---

## 8. 遗留 / 下一步（按优先级）

1. **拍板 §6**：渲染器走"确定性代码(Claude路线)"还是"AI 生成(codex路线)"。← 阻塞后续一切。
2. 据 §6 实现：要么写 `fillTemplateFromMarkdown` 裁剪/填充逻辑（让超集按 MD 裁出单梁/overhead，验证仍 1:1）；要么搭 AI 生成管线。
3. **输出可点击**：定 A（相对路径）/ B（单文件内联），消除"必须起服务器"。
4. 把 single-girder 的 build 指向修正（现指 product-superset，渲染器没好前会出大杂烩）。
5. overhead 页补 **CDP 全页几何 diff** 验证（目前仅结构级）。
6. commit 本轮成果。
7. 其余产品（6–14：臂式/葫芦/钩/卷筒…）抽样，确认无别的新模块类型。

---

*本记录截止于"渲染器代码 vs AI 路线之争"提出、待拍板时。后续进展另起记录。*
