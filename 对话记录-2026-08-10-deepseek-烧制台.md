# 对话记录 2026-08-10 · DeepSeek 烧制台（需求→设计→计划→执行）

> 参与：用户（拍板）+ Claude（控制/设计）+ 子代理群（实现与双审查）。
> 产出：spec `docs/superpowers/specs/2026-08-10-deepseek-burn-design.md`、计划（含 7 条执行期偏差记录）`docs/superpowers/plans/2026-08-10-deepseek-burn.md`、代码见下方 commit 链。CLAUDE.md 决策日志已收 2026-08-10 条。

## 一、需求讨论要点（用户问出来的真问题）

1. **「内容→格式化数据经过哪些东西」**——澄清了新 JSON 线的全貌：AI 只在「判断归类」烧一次，MD→JSON 是纯代码，schema 是写时闸门，geom 是收场验证。
2. **「为啥又出现 MD / block:KEY」**——MD 是存量资产不是新系统格式：当前这批是迁移（判断在旧系统已付过钱，md-to-json 继承）；D1 路（只有 HTML）与裸文章路都没有 MD 中间态。
3. **「能否接 LLM API」**——能，架构本来就按「AI 节点可替换」设计（闸门全在代码）。先例：i18n 翻译回路（emit/apply，产品期换 Lambda 原样上云）。
4. **「怎么规定 DeepSeek 做格式转换」**——三层：提示词契约 + JSON 模式锁格式 + 代码校验做终审退货。模型提议、代码裁决。
5. **「怎么判断 AI 会不会凑 specs_table 等不存在的字段」**——本次设计的最核心问题。答案：烧 = 搬运不是写作 → **逐字溯源硬校验**（规范化后必须是原文子串）是主力防线；规划阶段 span 表（块号引用）让「声称有这段」必须指得出原文位置；geom（有证人时）与人工抽看兜底。
6. **「发明版式是什么」**——用俄罗斯门机实例讲清：产品页 AI 只填值（结构现成）；post 每篇要连 .astro 骨架一起造（排版决定不在 JSON schema 里——外观锁），没有硬闸门 → 所以 v1 只接产品族。
7. **用户拍板**：先产品页；**这是另一条路，之前的不删除**；输入 C（裸文本+URL 都要）；只许 Node；要人看格式化后的数据再决定、有问题能改（改=现有编辑器，烧制台不建编辑功能）。

## 二、设计关键决策（spec 全文有）

- 模块拆分：纯逻辑 `burn-lib.mjs`（零网络零 AI 可单测）+ 编排 `deepseek-burn.mjs`（callAI 依赖注入）+ edit-server 薄壳端点。
- AI 产 markdown 不产树（mdToDoc 转换归代码，AI 爆不了骨架）；原文预编号（块号引用，span 校验=纯集合运算）。
- 字段分级：逐字（正文/规格）/ 相似（标题）/ 概括豁免+标出（SEO）/ 站级默认（chrome）/ 用户给（slug、产品名）。
- v1 图只进 gallery；slug 裸名；产物强制 draft；人预览过目才落盘。
- 明确不做：post 族、中途暂停确认段映射表、批量队列、多用户、云部署。

## 三、执行（子代理驱动，每任务 实现→spec审→质量审）

| 任务 | 内容 | 验收 | commit |
|---|---|---|---|
| T1 | burn-lib 骨架（切块/图清单/剥壳） | 4/4 | 7eb7927 |
| T2 | 字段目录双源核验 + 规划硬查（+T2.1 边界加固） | 23/23 | 1eb63e0 + a31b107 |
| T3 | 规范化/相似度/逐字溯源（+T3.1 钉） | 31→33/33 | 54454d1 |
| T4 | 组装 + 预览（+T4.1 渲染器无守卫字段修复） | 47/47 | 2309ec4 + ada4fe9 |
| T5 | 客户端 + 编排 + writeDraft/CLI（+T5.1 降级/快败等） | 65/65 | 5d46df8 + d02834e |
| T6 | CLI 无 key 冒烟（控制器直验） | — | 无改动 |
| T7 | edit-server 端点 + 控制台（+安全修复包） | 冒烟全绿 | 5ba71d7 + f4e4096 |
| T8 | 总验收 + 全量回归（geom 六页 1:1、根 check 绿） | 65/65 | b10844b |
| T9 | 文档（本文件 + 决策日志）；真 key 验收待用户给 key | — | 见 git log |

**审查抓到的最有价值问题（双审制生效的证据）**：
- T4 Critical：「失败段缺席」对 4 个组件无守卫字段不成立（实 build 实证），缺席即炸 rebuild 且毒草稿会祸害后续每次重建 → 改站级默认 + rebuild 失败自动删草稿。
- T5 Important：段级硬失败原会拖垮整次烧；4xx/截断白重试（key 错了要等 9 次 HTTP 才知情）。
- T7 Important：控制台报告区 XSS（被拒收的 AI 原文进 innerHTML）、iframe 无 sandbox、服务绑 0.0.0.0（配 key 后 url 参数=带读回 SSRF）。

## 四、运维注意

- 起编辑服务须 **node ≥22**（Astro rebuild 要求）：`nvm use 22 && npm run edit`（或等价）。
- key 配置：`DEEPSEEK_API_KEY` 环境变量（服务端）；可选 `DEEPSEEK_MODEL`（默认 deepseek-chat）、`DEEPSEEK_BASE_URL`。
- 真 key 验收步骤：计划 Task 9 Step 3（烧 overhead 裸文章对照手工 JSON、编辑器开改存、URL 路、验完删测试页重建）。

## 五、真 key 验收结果（2026-08-11 跑完）

**三条路全验，发现并修复 4 个真实问题：**

1. **裸文路 ✅**：overhead 裸文章 → 12 段全烧出（10 ok + 2 标题重修后过），uncovered 全空；与手工 JSON 对照段全覆盖（还多了手工漏掉的 main_features）；gallery 8 张=配图标记数。**编辑器开/改/存全通**（首次 playwright 乱码是合成键事件抢跑编辑器挂载的测试假像，insertText 复验干净）。
2. **URL 路 ✅（修后）**：①旧站尾部标记不是 `related-products` 而是 `#footer` div（该页无相关区元素，crane-related 只在 CSS 选择器里）→ 终止标记三选（related-products|crane-related|footer）；②DeepSeek 发明近似字段名（specification/application）→ 提示词教白名单逐字名 + 近似概念归并（应用场景→overview、介绍总结→summary_intro）。修后 cleanroom-overhead-cranes 全链 200（6 段、15 图、24 秒）。
3. **同字段多条目（验收发现 #1）**：DeepSeek 把同字段跨多块表达成每块一条目 → checkPlan 改合并（+阶段 2 消费合并后字段的闭环，实现者 DONE_WITH_CONCERNS 报出）。
4. **`/__burn-save` 实证**：落 draft → 双产物重建 → dist-edit 有页、生产 dist 无（状态门隔离正确）。验后测试页已清、双产物已重建还原。

**已知边界（用户决策项，不是 bug）**：内容超出 product@1 目录的页面（如铝门机的 Options/FAQ/Certifications 段）会被白名单拒收、烧不出来——要烧这类页需先扩目录（登记新段），这是 §2 引擎纪律的应有之义。

**运维**：起服务 `nvm use 22 && npm run edit`；key 配 `DEEPSEEK_API_KEY` 环境变量（服务端）。

## 六、选族 + 一把梭重构（2026-08-11 用户连续追问驱动）

**起因**：用户贴文章进产品族烧（错配）→ 加「选模版」；贴无空行阀门文章 → 块号越界报错 → 用户追问「为啥要切？AI 按内容直接组装 JSON 不对吗？」→ 用户要求「多找几个人讨论」→ 4 评审独立并行（输出质量/校验完备/工程成本/用户视角），结论收敛：

1. **选族（已上线）**：下拉「自动判断（默认）/产品页族/文章页族（待建灰）」；auto 判族未建干净拒绝（用户文章实测拒绝文案正确）；显式选族跳过判族。
2. **对照表/块号规划层整个退役**（4 评审一致：实战两次失败全炸在这层；用户看不懂这层报告=人工闸门名存实亡；成本 13 次调用→1 次）。
3. **终态 = 一把梭 + 代码验收**：AI 一次出整页 JSON；机器验收四道——防编（逐句规范化子串查全文）、**防错位（命中位置审计：同段复用/顺序颠倒告警；位置机器算，不经 AI 认领）**、防漏（未用段落反查列出）、白名单；失败格单独重烧（喂回拒收原因，防温度 0 确定性重放）；截断干净报错（自动逐格降级在 roadmap）。
4. **报告层纪律（用户视角评审）**：段号/阶段/重试等内部词永不上桌；只给预览 + 哪格哪句对不上（带原句）+ 未用段落 + fieldMap（格子←原文第几段）。
5. **已知口径**：unused 是整段包含判定，部分采用的段落会进未用清单（保守，供人工判断）；顺序审计只对正文段格（chrome 格无顺序语义，否则系统性误报）。

提交：重构 d6dc545 → 修复轮 516828a（重烧喂回原因/硬失败降级/带噪提示恢复/契约动态化）→ 审查修复轮（顺序误报过滤/fieldMap/oneshot 8192/audit 直接钉）。验收 67+5 条全绿；geom 六页 1:1。

## 七、后续

真枪验收（两阶段版）已通过；一把梭重构后需真 key 复验一轮（复验对象：无空行的阀门文章——原块号越界案例）。post 族烧组件（发明版式）等 v1 跑顺再议；超集目录扩段（如 faq/applications）按实际烧到的页面类型再定。
